---
phase: 01-cms-app
plan: 01-02
plan_name: "UI — public blog (grid/article/category/tag/search), markdown editor with live preview, admin category/tag management, seed v2, README demo guide"
status: draft
type: execute
wave: 2
depends_on: [01-01]
files_created:
  - components/posts/markdown-components.tsx
  - __tests__/markdown.test.tsx
  - components/blog/blog-card.tsx
  - components/blog/cover-image.tsx
  - components/blog/blog-search.tsx
  - components/blog/category-badge.tsx
  - components/blog/tag-chip.tsx
  - components/blog/related-posts.tsx
  - components/blog/markdown-content.tsx
  - app/(main)/blog/page.tsx
  - app/(main)/blog/loading.tsx
  - app/(main)/blog/error.tsx
  - app/(main)/blog/[slug]/page.tsx
  - app/(main)/blog/category/[slug]/page.tsx
  - app/(main)/blog/tag/[slug]/page.tsx
  - app/(main)/blog/search/page.tsx
  - components/posts/editor-shell.tsx
  - components/posts/markdown-preview.tsx
  - components/posts/tags-input.tsx
  - components/posts/cover-upload.tsx
  - components/admin/category-dialog.tsx
  - components/admin/tag-dialog.tsx
  - components/admin/cms-category-table.tsx
  - components/admin/cms-tag-table.tsx
  - app/admin/categories/page.tsx
  - app/admin/tags/page.tsx
files_modified:
  - package.json
  - app/globals.css
  - app/not-found.tsx
  - app/(main)/posts/new/page.tsx
  - app/(main)/posts/[id]/edit/page.tsx
  - components/posts/posts-table.tsx
  - components/layout/admin-shell.tsx
  - app/admin/page.tsx
  - components/layout/site-header.tsx
  - components/layout/mobile-nav.tsx
  - lib/site.ts
  - scripts/seed.ts
  - README.md
autonomous: true
requirements: [CMS-01, CMS-02, CMS-03, CMS-04, CMS-05, CMS-06, CMS-07, CMS-08]
must_haves:
  truths:
    - Guests can browse /blog, /blog/[slug], /blog/category/[slug], /blog/tag/[slug], and /blog/search without logging in; only status = 'published' posts ever render
    - Drafts and unknown slugs return the identical 404 on public routes (no enumeration); drafts never leak through category/tag/search pages
    - The editor creates and edits posts with a live markdown preview; preview and article render identically because both consume the one shared markdown-components map + .article-body class (no drift)
    - Admin can create/rename/delete categories and tags under /admin with dialogs + tables; the admin sidebar gains a Content group; header nav shows Blog to guests and authenticated users
    - npm run seed populates 5 realistic posts (never lorem ipsum), 4 categories, 6-8 tags with picsum covers; README documents the demo guide
  artifacts:
    - app/(main)/blog/** (page, [slug], category/[slug], tag/[slug], search, loading, error)
    - components/blog/* (blog-card, cover-image, blog-search, category-badge, tag-chip, related-posts, markdown-content)
    - components/posts/* (markdown-components, editor-shell, markdown-preview, tags-input, cover-upload)
    - components/admin/* (category-dialog, tag-dialog, cms-category-table, cms-tag-table) + app/admin/categories/page.tsx + app/admin/tags/page.tsx
    - app/globals.css (.article-body), package.json (react-markdown 10.1.0 + remark-gfm 4.0.1)
    - scripts/seed.ts (v2), README.md (Demo Guide)
  key_links:
    - markdown-components.tsx is imported by BOTH markdown-content (server) and markdown-preview (client) — the single source preventing visual drift (RESEARCH Pattern 4 / Pitfall 5)
    - editor-shell hidden fields (status, categoryId, tags, coverImage) must match parsePost's formData.get keys from 01-01 Task 4 exactly — the FormData key contract is coverImage (parsePost reads formData.get("coverImage") per 01-01 Task 4); only the DB column is cover_image, mapped by the action on INSERT
    - cover-upload's 3 MB client cap must mirror /api/uploads MAX_BYTES (01-01 Task 4) — the UI-SPEC copy now reads "under 3 MB"
    - Every public query hard-filters WHERE status = 'published' (RESEARCH Pitfall 4) — the 01-01 migration made status the source of truth
    - Seed post content must not rely on raw HTML (react-markdown escapes it — UI-SPEC security note)
---

<objective>
Build all Phase 1 UI on the 01-01 data layer: the shared markdown pipeline (components map + .article-body class, proven XSS-safe by a component test), the public blog (grid, article, category/tag filters, search), the markdown editor with live preview replacing post-form, admin category/tag management, header/admin navigation updates, seed v2 demo content, and the README Demo Guide.

Purpose: This is where the phase's success criteria become user-visible — a guest can read published stories, search, and filter; an authenticated user can author markdown with a live preview and manage taxonomy; the seed makes the demo look real on first load.

Output: All 5 public blog routes + 7 blog components, the editor shell + 3 editor components, 2 admin pages + 4 admin components, nav updates, 5 seeded posts with categories/tags, README Demo Guide.

<scope>
- Public UI: app/(main)/blog/** (all routes under the (main) AppShell — public by proxy construction, do NOT touch proxy.ts)
- Editor: components/posts/editor-shell + markdown-preview + tags-input + cover-upload; rewiring /posts/new and /posts/[id]/edit; extending posts-table; DELETING post-form.tsx (superseded)
- Admin: /admin/categories, /admin/tags + dialogs/tables; admin-shell Content group; admin overview StatCards
- Nav: lib/site.ts defaultNav + site-header desktopNav (guest AND auth branches) + mobile-nav links — note these are three separate hardcoded lists
- Content: seed v2 (5 posts, 4 categories, 6-8 tags, post_tags, picsum covers) + README Demo Guide
- NOT in this plan: any data-layer changes (migration, schemas, actions, upload route) — 01-01 owns them; taxonomy actions are consumed, not created, here
</scope>
</objective>

> **Executor note (scope W-2):** Task 2 (public blog) touches 13 files. Watch the context budget during Task 2 — if it runs low, split its execution into two sub-sessions (pause and resume) rather than compressing scope; the remaining tasks stay cohesive as planned.

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-cms-app/01-CONTEXT.md (locked decisions — newspaper Features styling, clean URLs, serif reading experience, admin reuses shell)
@.planning/phases/01-cms-app/01-RESEARCH.md (AUTHORITATIVE — Pattern 2 public routes, Pattern 3 search page, Pattern 4 shared markdown pipeline, Pattern 6 editor data contract, Pitfalls 4/5/6, Key Decisions 4/5/10/11, Code Examples: markdown-content + blog list query)
@.planning/phases/01-cms-app/01-UI-SPEC.md (THE design contract — Page Specs 1-9, Component Inventory, Copywriting Contract, Interaction & State Patterns, Accessibility, Visual Quality Bar; upload copy now reads "under 3 MB" per 01-01 amendment)

@lib/blog.ts (slugify/escapeLike/parseTags/readingTime/excerpt from 01-01 — import, do not reimplement)
@lib/validate.ts (extended postSchema + categorySchema/tagSchema from 01-01 — editor hidden field contract)
@app/(main)/posts/actions.ts (createPost/updatePost + taxonomy actions from 01-01 — bind via useActionState)
@app/api/uploads/route.ts (3 MB cap from 01-01 — cover-upload posts here)
@components/posts/posts-table.tsx, @components/layout/admin-shell.tsx, @app/admin/page.tsx, @app/not-found.tsx (files being extended)
@components/layout/site-header.tsx, @components/layout/mobile-nav.tsx, @lib/site.ts (nav — three hardcoded lists, all need Blog)
</context>

<tasks>

<task type="tdd" id="01-02-01">
  <name>Task 1: TDD shared markdown pipeline — install react-markdown + remark-gfm, markdown-components.tsx, .article-body, XSS regression test</name>
  <files>package.json, __tests__/markdown.test.tsx, components/posts/markdown-components.tsx, app/globals.css</files>
  <action>
    RED — First install the two locked dependencies (RESEARCH Standard Stack, legitimacy Approved — no checkpoint): `npm install react-markdown@10.1.0 remark-gfm@4.0.1` (pinned exact versions; verify package.json shows no ^ range and `npm view react-markdown version` confirms 10.1.0). Then create __tests__/markdown.test.tsx (jsdom + RTL already configured): render <Markdown remarkPlugins={[remarkGfm]} components={markdownComponents}> with content containing "<script>alert(1)</script>", "<img src=x onerror=alert(1)>", and a link "[x](javascript:alert(1))"; assert container.querySelector("script") is null, no element carries an onerror attribute, and no anchor href starts with "javascript:". Import markdownComponents from "@/components/posts/markdown-components" and remarkGfm + Markdown from the new packages. Run `npx vitest run __tests__/markdown.test.tsx` — MUST FAIL (module missing). This test IS the XSS posture proof (RESEARCH Validation Architecture: react-markdown escapes raw HTML by default; no rehype-raw; defaultUrlTransform strips javascript:).
    GREEN — Create components/posts/markdown-components.tsx as a plain .tsx module with NO "use client"/"server-only" directives (safe for BOTH the server markdown-content and the client markdown-preview — RESEARCH Pattern 4). Export markdownComponents: a react-markdown components map implementing the UI-SPEC Markdown content styles table as className strings — h1 remapped to h2 (a11y rule: never two H1s on the article page; RESEARCH Open Question 3 resolution), h2 ("text-2xl font-semibold tracking-tight mt-8 mb-3"), h3 ("text-xl font-semibold tracking-tight mt-6 mb-2"), p (plain — spacing from .article-body CSS), a ("text-primary underline underline-offset-4 hover:text-primary/80"), strong ("font-semibold"), em (serif italic), blockquote ("border-l-2 border-primary pl-4 italic text-muted-foreground my-4"), ul/ol ("pl-5 mb-4" with nested pl-5), hr ("my-8 border-t border-border"), code inline ("font-mono text-sm bg-muted px-1.5 py-0.5 border border-border"), pre ("font-mono text-sm bg-card border border-border p-4 overflow-x-auto mb-4"), img ("w-full border border-border my-6" — plain <img>, never next/image, per UI-SPEC), table/th/td ("w-full text-sm border-collapse my-4" / "border border-border px-3 py-2" / th "font-semibold text-left"). Also export remarkPlugins = [remarkGfm] as a shared const. Then append ONE .article-body class to app/globals.css implementing the same typography table token-based (no @tailwindcss/typography plugin): p 16px serif 1.75 line-height mb-4 text-foreground; drop cap ONLY via .article-body > p:first-child::first-letter (text-5xl, font-bold, font-serif, float-left, mr-2, leading-[0.8], text-primary — UI-SPEC backstop: applies only when the first element is a paragraph); list markers text-muted-foreground; nested lists pl-5. Scope EVERY rule under .article-body — never style bare p/h2/table globally (RESEARCH Pitfall 5 CSS bleed). Run the test — MUST PASS.
    REFACTOR — Verify the map's class strings and the CSS use identical tokens (visual-drift prevention is the whole point). Re-run the test.
    Grep gate: `grep -rn "rehype-raw" app components lib` and `grep -rn "dangerouslySetInnerHTML" app components lib` must both return ZERO matches (RESEARCH Anti-Patterns — the single biggest XSS regression risk).
  </action>
  <verify>
    <automated>npx vitest run __tests__/markdown.test.tsx && npm run test</automated>
    <automated>grep -rn "rehype-raw\|dangerouslySetInnerHTML" app components lib | wc -l</automated>
  </verify>
  <done>
    RED: XSS test written, fails on missing module
    GREEN: react-markdown@10.1.0 + remark-gfm@4.0.1 installed pinned; markdown-components.tsx exports the map (h1→h2 remap) + remarkPlugins; .article-body in globals.css with drop cap and token-based styles; XSS test passes (script/onerror/javascript: all neutralized)
    REFACTOR: full suite green; zero rehype-raw / dangerouslySetInnerHTML references
  </done>
</task>

<task type="execute" id="01-02-02">
  <name>Task 2: Public blog — /blog grid, category/tag pages, /blog/search, grid components, header nav Blog link</name>
  <files>app/(main)/blog/page.tsx, app/(main)/blog/loading.tsx, app/(main)/blog/error.tsx, app/(main)/blog/category/[slug]/page.tsx, app/(main)/blog/tag/[slug]/page.tsx, app/(main)/blog/search/page.tsx, components/blog/blog-card.tsx, components/blog/cover-image.tsx, components/blog/blog-search.tsx, components/blog/category-badge.tsx, components/blog/tag-chip.tsx, components/layout/site-header.tsx, components/layout/mobile-nav.tsx, lib/site.ts</files>
  <action>
    Nav first (Blog must be reachable the moment the pages exist): lib/site.ts defaultNav gains { label: "Blog", href: "/blog" } after Home (UI-SPEC Layout Structure). components/layout/site-header.tsx desktopNav: add { label: "Blog", href: "/blog" } to BOTH branches — the guest branch (currently only Home) and the authenticated branch (Home · Blog · Posts · Admin). components/layout/mobile-nav.tsx links: add Blog to both user and guest branches. proxy.ts is NOT touched (RESEARCH Pattern 2 — /blog is unmatched, public by construction).

    Every page below: `export const dynamic = "force-dynamic"` (AGENTS.md hard convention) and `WHERE status = 'published'` on every query (RESEARCH Pitfall 4 — drafts never leak). params/searchParams are Promises in Next 16 — await them.

    /blog (UI-SPEC Page 1) — single-round-trip query per RESEARCH Code Example: SELECT p.id, p.title, p.slug, p.cover_image, p.content, COALESCE(p.published_at, p.created_at) AS published_at, u.name AS author_name, c.slug AS category_slug, c.name AS category_name, COALESCE(array_agg(t.name) FILTER (WHERE t.name IS NOT NULL), '{}') AS tag_names, COALESCE(array_agg(t.slug) FILTER (WHERE t.slug IS NOT NULL), '{}') AS tag_slugs FROM posts p JOIN users u ON u.id = p.author_id LEFT JOIN categories c ON c.id = p.category_id LEFT JOIN post_tags pt ON pt.post_id = p.id LEFT JOIN tags t ON t.id = pt.tag_id WHERE p.status = 'published' GROUP BY p.id, u.name, c.slug, c.name ORDER BY COALESCE(p.published_at, p.created_at) DESC (GROUP BY p.id is valid — Postgres PK functional dependency). Layout: kicker row — mono "The Blog" label (text-primary, tracking-[0.28em], border-b-2 border-primary) left + blog-search right; hairline rule row (h-px bg-muted-foreground/40); H1 "Latest stories" (Display role); grid gap-6 md:grid-cols-2 lg:grid-cols-3 with column hairlines (md:border-l border-muted-foreground/40 on cards 2+, md:border-t on row wraps — landing Features treatment). Card data: compute excerpt(content) and readingTime(content) from lib/blog.ts server-side before passing to blog-card. Empty state: EmptyState with Newspaper icon, heading "No stories yet", body "Published posts will appear here. Check back soon.", NO CTA (UI-SPEC copy).

    /blog/category/[slug] (UI-SPEC Page 3) — await params; first look up the category by slug (SELECT id, name FROM categories WHERE slug = ${slug}) → rows.length === 0 → notFound() (unknown slug = 404, identical to draft-only category reading as empty-state, never a leak). Query posts with the same shape as /blog plus AND c.slug = ${slug}. Kicker "Category" (mono primary) + hairline; H1 = category name; description "N stories filed under {name}." (muted); blog-search right-aligned in the header row; identical Features grid. Zero published posts → EmptyState "No stories in {name} yet" / "Check back soon, or browse all stories." / CTA "Browse all stories" → /blog.

    /blog/tag/[slug] (UI-SPEC Page 4) — same shape: look up tag by slug → notFound() if missing; posts query JOINs post_tags/tags with AND t.slug = ${slug}; kicker "Tag", H1 = tag name, description "N stories tagged "{name}"."; same grid + empty-state copy.

    /blog/search (UI-SPEC Page 5, RESEARCH Pattern 3 — a PAGE, not a route handler): const q = (await searchParams).q?.trim() ?? ""; if (!q || q.length > 100) redirect("/blog") (RESEARCH Pitfall 6 — empty/whitespace/oversized queries never render). const pattern = `%${escapeLike(q)}%` (import escapeLike from lib/blog.ts). One parameterized query with DISTINCT (dedupes posts matching multiple tags): SELECT DISTINCT p.id, p.title, p.slug, p.cover_image, p.content, COALESCE(p.published_at, p.created_at) AS published_at, u.name AS author_name, c.slug AS category_slug, c.name AS category_name, COALESCE(array_agg(t.name) FILTER (WHERE t.name IS NOT NULL), '{}') AS tag_names, COALESCE(array_agg(t.slug) FILTER (WHERE t.slug IS NOT NULL), '{}') AS tag_slugs FROM posts p JOIN users u ON u.id = p.author_id LEFT JOIN categories c ON c.id = p.category_id LEFT JOIN post_tags pt ON pt.post_id = p.id LEFT JOIN tags t ON t.id = pt.tag_id WHERE p.status = 'published' AND (p.title ILIKE ${pattern} OR p.content ILIKE ${pattern} OR c.name ILIKE ${pattern} OR t.name ILIKE ${pattern}) GROUP BY p.id, u.name, c.slug, c.name ORDER BY COALESCE(p.published_at, p.created_at) DESC. Layout: header row — kicker "The Blog" + blog-search prefilled defaultValue={q}; H1 "Search results"; count line (mono muted text-sm, "N results for "query"" / singular "1 result for "query""); then the Features grid. Zero results → EmptyState with SearchX icon, heading "No results for "query"", body "Try different keywords, or browse all stories.", CTA "Browse all stories" → /blog.

    Components (UI-SPEC Component Inventory, all under components/blog/):
    - blog-card.tsx (server): props { post } = { title, slug, excerpt, category: {name,slug} | null, coverImage: string | null, publishedAt, authorName, readingTime }. Renders cover-image (aspect-[3/2], border-b border-border, null-safe) → category-badge or nothing when uncategorized → H3 serif title Link to /blog/{slug} (line-clamp-2, hover:text-primary) → excerpt (line-clamp-3, text-muted-foreground) → dateline row (font-mono text-[10px] uppercase tracking-[0.12em] text-muted-foreground: formatted date · "· N min read"). Column hairlines come from the parent grid.
    - cover-image.tsx (client): props { src, alt, className? } — plain <img> (object-cover) with onError → setState(null) so broken/mock URLs render nothing (text-only card is valid — RESEARCH A5). Always set alt (UI-SPEC a11y: alt={`Cover for ${post.title}`}).
    - blog-search.tsx (client): props { defaultValue? } — <form method="GET" action="/blog/search">; input name="q" type="search" with Search icon left, aria-label "Search the blog", placeholder "Search the blog…", maxLength={100}, defaultValue; submits on Enter or the icon button; w-64 on desktop, full width on mobile. No client fetch, no debounce (server-rendered results per UI-SPEC §5).
    - category-badge.tsx (server): props { name, slug } — mono 10px uppercase tracking-[0.24em] text-primary with border-b-2 border-primary, Link to /blog/category/{slug}. Kicker role, not a pill.
    - tag-chip.tsx (server): props { name, slug } — Badge variant="outline" with rounded-none, Link to /blog/tag/{slug}, hover bg-muted.

    loading.tsx: blog-grid skeleton — 9 cards each = aspect-[3/2] animate-pulse bg-muted block + 3 text lines, aria-label "Loading stories". error.tsx: "use client" boundary → error-state + "Try again" button calling reset().
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/blog</automated>
  </verify>
  <done>
    - /blog renders the Features grid of published posts (guest, no cookie); drafts absent; empty DB shows the "No stories yet" empty state
    - /blog/category/[slug] and /blog/tag/[slug] filter correctly; unknown slug → 404; zero-published taxonomy → empty-state with CTA
    - /blog/search?q=… returns results across title/content/category/tag; % and _ are treated literally (escapeLike); empty q or > 100 chars redirects to /blog; zero results show the SearchX empty state
    - Blog appears in the header and mobile nav for BOTH guests and authenticated users; proxy.ts unchanged
    - npm run build, npx tsc --noEmit, npm run lint pass
  </done>
</task>

<task type="execute" id="01-02-03">
  <name>Task 3: Article page + markdown editor — /blog/[slug], related posts, editor-shell with live preview, rewired new/edit pages, extended posts table</name>
  <files>app/(main)/blog/[slug]/page.tsx, components/blog/markdown-content.tsx, components/blog/related-posts.tsx, app/not-found.tsx, components/posts/editor-shell.tsx, components/posts/markdown-preview.tsx, components/posts/tags-input.tsx, components/posts/cover-upload.tsx, app/(main)/posts/new/page.tsx, app/(main)/posts/[id]/edit/page.tsx, components/posts/posts-table.tsx, app/(main)/posts/page.tsx</files>
  <action>
    Part A — Article page (UI-SPEC Page 2): /blog/[slug] — force-dynamic; await params; query the post by slug WITH status = 'published' (WHERE p.slug = ${slug} AND p.status = 'published') joined with category + tags (same array_agg shape as Task 2); rows.length === 0 → notFound() — identical 404 for unknown AND draft slugs (RESEARCH Pitfall 4, no enumeration). Layout: kicker row — category-badge (when categorized) + tag-chip list (when tagged); H1 title (serif text-4xl sm:text-5xl font-bold tracking-tight, max-w-3xl); byline dateline row (font-mono text-[10px] uppercase tracking-[0.12em] text-muted-foreground: "{Author} · {date} · {N} min read" where N = readingTime(content) computed server-side); cover-image (max-w-3xl, aspect-[3/2], border); article body column (max-w-2xl mx-auto) → <MarkdownContent content={post.content} />; hairline + related-posts section.

    components/blog/markdown-content.tsx (server — NO "use client") exactly per RESEARCH Code Example: <div className="article-body"><Markdown remarkPlugins={[remarkGfm]} components={markdownComponents}>{content}</Markdown></div> — importing the map + plugin const from components/posts/markdown-components (Task 1). This IS the shared pipeline — no second style set (RESEARCH Pitfall 5).

    components/blog/related-posts.tsx (server): props { categorySlug: string | null, currentPostId: string }. When categorySlug is null or the query returns zero rows → render null (documented omission — UI-SPEC: section OMITTED when empty, never an empty state). Query: SELECT p.id, p.title, p.slug, p.cover_image, COALESCE(p.published_at, p.created_at) AS published_at FROM posts p WHERE p.category_id = (SELECT id FROM categories WHERE slug = ${categorySlug}) AND p.status = 'published' AND p.id <> ${currentPostId} ORDER BY p.created_at DESC LIMIT 3. Renders mono primary "Related coverage" label + hairline + grid gap-6 md:grid-cols-3 of compact rows (kicker, serif title link, dateline — no cover).

    app/not-found.tsx: add the blog 404 copy (UI-SPEC) — primary Button "Back to blog" (→ /blog) plus ghost Button "Back to home" (→ /). The existing heading "Page not found" stays.

    Part B — Editor (UI-SPEC Pages 6/7, Layout Structure, Interaction §1/§3/§4; RESEARCH Pattern 6 data contract):
    - components/posts/editor-shell.tsx (client): props { mode: "create" | "edit", post?: { id, title, content, status, slug, categoryId, coverImage, tagNames: string[] } | null, categories: { id, name }[] }. Full form: Title input; Slug input (mono, max-w-sm) with helper "Auto-generated from the title — edit to customize." and auto-derivation from the title on blur via slugify (lib/blog.ts) when the user hasn't manually edited it; grid gap-4 md:grid-cols-2 → Category select (options from categories prop, value "" = "No category", helper "Used for the blog kicker and related posts.") + Tags (tags-input, helper "Press Enter or comma to add a tag."); Cover image (cover-upload); Status segmented control (role="radiogroup", two role="radio" buttons Draft|Published with aria-checked + arrow-key nav, active segment bg-background border shadow-sm — paper-in-paper, NO primary fill; helper "Drafts are visible only to you."); Content markdown split (grid gap-6 lg:grid-cols-2 — left: Label "Content" + Textarea font-mono text-sm min-h-[320px] lg:h-[65vh]; right: markdown-preview lg:h-[65vh] overflow-y-auto); action row (flex gap-4 justify-end). Hidden fields carrying non-native inputs: status, categoryId, tags (comma-joined), coverImage (name="coverImage" — must equal parsePost's formData.get("coverImage") key from 01-01 Task 4; the action maps it to the cover_image column). useActionState bound to createPost (mode create) / updatePost (mode edit; hidden id input); blocking submit (pending → disabled + Loader2); inline field errors via state.errors (aria-invalid + aria-describedby per shadcn form pattern); destructive Alert for state.message, with "Back to posts" link when the message is "This post no longer exists."; success → toast + router.push("/posts") + router.refresh() — toast keyed off the submitted status: "Draft saved." / "Post published." (create) and "Post updated." (edit of published); buttons: create → "Save draft" (secondary) + "Publish" (primary); edit → "Save draft" (secondary when status=draft) + "Save changes" (primary when status=published). No autosave, no dirty flags, no beforeunload (UI-SPEC Interaction §2).
    - components/posts/markdown-preview.tsx (client): props { markdown: string } — renders <Markdown remarkPlugins={[remarkGfm]} components={markdownComponents}>{markdown}</Markdown> inside div.article-body, wrapped in a bg-card border border-border pane with a mono 10px uppercase "Preview" label + hairline; aria-label="Markdown preview"; NOT aria-live (per-keystroke SR spam — UI-SPEC a11y §2); memoize the rendered tree.
    - components/posts/tags-input.tsx (client): props { defaultValue: string[], error? } — chips row (Badge variant="secondary" with X remove button, aria-label "Remove tag {name}") + text input; Enter or comma adds (trim, case-insensitive dedupe, max 8 → inline error "Add up to 8 tags."), keydown preventDefault so Enter never submits the form; Backspace on empty input removes the last chip; serialized into a hidden input name="tags" as a comma-joined string (aria-invalid + aria-describedby wired to the error prop).
    - components/posts/cover-upload.tsx (client): props { defaultValue?: string } — "Upload cover" button (ImagePlus icon) toggling a visually-hidden focusable <input type="file" accept="image/*">; client validates image type and size ≤ 3 MB ("File must be an image under 3 MB." — the 01-01 UI-SPEC amendment; mirror the route's MAX_BYTES); POST /api/uploads with FormData field "file"; pending → disabled + Loader2 + "Uploading…"; 201 → store url in hidden input name="coverImage" + preview pane; mock-storage URLs (https://mock.storage/...) render a muted placeholder box with ImageIcon + mono note "Mock cover — URL only"; "Remove" ghost button (X icon) clears the hidden field; failure → destructive Alert "Cover upload failed. Try again." (upload stays retryable, form data preserved); success → no toast (field-level feedback only).

    Part C — Wiring: app/(main)/posts/new/page.tsx fetches categories (SELECT id, name FROM categories ORDER BY name) and renders <EditorShell mode="create" categories={...} /> inside the existing card (max-w-5xl p-6 per UI-SPEC Layout). app/(main)/posts/[id]/edit/page.tsx: extend the ownership-scoped fetch to SELECT id, title, content, status, slug, category_id, cover_image + tag names (array_agg) FROM posts WHERE id = ${id} AND author_id = ${user.id} (still notFound() when not the owner — IDOR posture inherited); also fetch categories; render <EditorShell mode="edit" post={...} categories={...} />. DELETE components/posts/post-form.tsx (superseded — grep gate below proves no dangling imports). components/posts/posts-table.tsx: PostRow gains status (string, from Task 1 of 01-01), categoryName (string | null), tagNames (string[]); Status badge shows Published (variant default) / Draft (variant secondary); add Category and Tags columns (Tags: up to 2 chips or truncated comma-joined text, overflow handled); app/(main)/posts/page.tsx query gains the category + array_agg tags JOINs (posts table shows every post incl. own drafts — unchanged visibility rule).
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint && npm run test</automated>
    <automated>grep -rn "post-form" app components | wc -l</automated>
  </verify>
  <done>
    - /blog/[slug] renders a published post with drop cap, kicker, chips, byline, cover, related coverage (omitted when empty); draft slug and unknown slug both 404 with "Back to blog"
    - Editor create: title/slug/category/tags/cover/status/content flow into createPost; live preview mirrors the article typography (same map + class); "Save draft" and "Publish" behave per contract; duplicate slug shows the destructive Alert
    - Editor edit: full prefill incl. tags and cover; ownership-scoped fetch 404s for other users' posts; posts-table shows status badge + category + tags columns; post-form.tsx deleted with zero imports remaining
    - npm run build, npx tsc --noEmit, npm run lint, npm run test pass
  </done>
</task>

<task type="execute" id="01-02-04">
  <name>Task 4: Admin category/tag management + admin shell Content group + seed v2 + README Demo Guide</name>
  <files>app/admin/categories/page.tsx, app/admin/tags/page.tsx, components/admin/category-dialog.tsx, components/admin/tag-dialog.tsx, components/admin/cms-category-table.tsx, components/admin/cms-tag-table.tsx, components/layout/admin-shell.tsx, app/admin/page.tsx, scripts/seed.ts, README.md</files>
  <action>
    Part A — Admin shell + overview (UI-SPEC Layout Structure): components/layout/admin-shell.tsx gains a second nav group "Content" below "Platform" — Categories (→ /admin/categories, FolderOpen icon) and Tags (→ /admin/tags, Hash icon). app/admin/page.tsx adds two StatCards (Categories count, Tags count — icons FolderOpen / Hash) to the existing grid (5 cards total; counts from SELECT count(*) on categories and tags in the existing Promise.all).

    Part B — Admin taxonomy pages (UI-SPEC Pages 8/9): both pages force-dynamic, under the existing /admin layout (proxy + layout auth — actions ALSO re-verify per 01-01). /admin/categories: page-header "Categories" / "Manage blog categories" with action "New category" (Plus) opening category-dialog in create mode; card wrapping cms-category-table. Query: SELECT c.id, c.name, c.slug, c.created_at, count(p.id)::int AS post_count FROM categories c LEFT JOIN posts p ON p.category_id = c.id GROUP BY c.id ORDER BY c.name. /admin/tags: page-header "Tags" / "Manage blog tags", action "New tag"; query LEFT JOIN post_tags for counts (GROUP BY t.id ORDER BY t.name).

    components/admin/category-dialog.tsx (client): props { mode: "create" | "rename", category?: { id, name, slug } | null, trigger: ReactNode } — shadcn Dialog; fields Name + Slug (slug auto-derived from name on blur via slugify, mono input, helper per contract); inline errors via action state; submit pending spinner; binds createCategory (create) / renameCategory (rename, hidden id); success → toast ("Category created." / "Category renamed.") + close + router.refresh(). Duplicate slug → destructive Alert "A category with this slug already exists." (23505 message from the action).
    components/admin/tag-dialog.tsx: identical shape binding createTag/renameTag; create toast "Tag added." per UI-SPEC copy contract.
    components/admin/cms-category-table.tsx (client): props { categories: { id, name, slug, createdAt, postCount }[], loading?: boolean } — columns Name (with muted "{n} post{s}" suffix), Slug (mono muted, truncate max-w-[200px]), Created (Intl.DateTimeFormat medium), Actions dropdown (Rename → category-dialog rename; Delete → AlertDialog with copy "Delete category?" / "Posts in this category will become uncategorized. This action cannot be undone." / destructive "Delete" button, blocking pending); row hover bg-muted/50; skeleton variant (5 × h-12 rows); empty → EmptyState "No categories yet" / "Create a category to organize the blog." / CTA "New category". Delete binds deleteCategory → toast "Category deleted." + refresh.
    components/admin/cms-tag-table.tsx: same shape; delete copy "Delete tag?" / "The tag will be removed from all posts. This action cannot be undone."; empty → "No tags yet" / "Tags help readers find related stories." / CTA "New tag". Wrap both tables in overflow-x-auto min-w-[560px].

    Part C — Seed v2 (scripts/seed.ts, RESEARCH Key Decision 10): enrich the 3 existing posts and add 2 new ones — "The Nature Theme — how the newspaper look works" (design/theme guide) and "Posts CRUD — from blank form to published story" (CRUD tutorial) — all 5 with hardcoded kebab-case slugs, status "published", published_at = now() - interval 'N days' (deterministic staggered dates), cover_image = https://picsum.photos/seed/{slug}/800/533. Content must exercise the typography set (h2/h3, lists, code blocks, blockquotes, links, tables) and MUST NOT rely on raw HTML (react-markdown escapes it — UI-SPEC security note). Never lorem ipsum. Add CATEGORIES (4, fixed UUIDs, e.g. template-guide, mock-services, deployment, design — names human-readable) and TAGS (6-8, fixed UUIDs) arrays; upsert both with ON CONFLICT (slug) DO UPDATE SET name = EXCLUDED.name (categories) / ON CONFLICT (id) DO NOTHING (tags with slug conflict — pick one conflict target per table and stay consistent). Assign category_id per post and add POST_TAGS join rows (INSERT ... ON CONFLICT DO NOTHING). Posts upsert gains slug, status, category_id, cover_image, published_at in the DO UPDATE set. Keep the TABLES array (already includes categories/tags/post_tags from 01-01) and the 200 MB gate.
    (Fixed UUIDs for the two new posts follow the existing 1111…/2222…/3333… pattern — continue the sequence.)

    Part D — README.md: append a "Demo Guide" section — demo credentials (demo@example.com / demo1234), a walkthrough (create a post in markdown with the live preview → publish → see it on /blog; search by title/content; browse category/tag pages; upload a cover; manage categories/tags in /admin), the honest-mock note (upload URLs are simulated — mock.storage URLs never resolve, seed covers use picsum.photos), and how to run (npm run seed, npm run dev).
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm run lint && npm run test && npm run seed</automated>
    <automated>grep -ci "lorem" scripts/seed.ts</automated>
  </verify>
  <done>
    - /admin/categories and /admin/tags list rows with post counts; create/rename/delete flows work end-to-end with toasts + refresh; delete copies match the UI-SPEC contract; duplicate slug alerts surface
    - Admin sidebar shows the Content group; overview shows 5 stat cards; header + mobile nav include Blog (from Task 2)
    - npm run seed upserts 5 posts (status published, slugs, picsum covers), 4 categories, 6-8 tags, and post_tags joins idempotently; zero "lorem" in seed.ts; size report under 200 MB
    - README contains the Demo Guide with credentials and the mock-honesty note
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest → public /blog/** | Untrusted markdown content (post bodies) renders here — the article page is the XSS surface |
| client editor → server actions | Untrusted FormData (markdown, slugs, tags) crosses to the 01-01 actions |
| client → /api/uploads | Untrusted file upload (validated client-side as UX; the route is the enforcement) |
| admin → taxonomy pages/actions | Authenticated management surface — proxy + layout + action re-checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering | markdown rendering (article + preview) | critical | mitigate | react-markdown safe-by-default: raw HTML escaped, NO rehype-raw, NO dangerouslySetInnerHTML (grep-gated); regression test in T-01-02-01 asserts script/onerror/javascript: never reach the DOM (UI-SPEC security note + RESEARCH Pattern 4) |
| T-01-02-02 | Tampering | markdown links/images | medium | mitigate | defaultUrlTransform protocol allowlist strips javascript: (covered by the XSS test); seed content avoids raw HTML |
| T-01-02-03 | Information Disclosure | public /blog routes vs drafts | high | mitigate | Every public query hard-filters WHERE status = 'published' (grid, article, category, tag, search); draft slug → same notFound() as unknown slug (RESEARCH Pitfall 4) |
| T-01-02-04 | Information Disclosure | draft/unknown slug + taxonomy enumeration | medium | mitigate | notFound() for unknown category/tag slugs; draft-only categories read as empty-state (never list drafts, never reveal existence) |
| T-01-02-05 | Information Disclosure | edit prefill / ownership | high | mitigate | Ownership-scoped fetch (id AND author_id) → notFound() for non-owners (inherited pattern, extended to the new fields); generic 404, no enumeration |
| T-01-02-06 | Spoofing | editor + taxonomy mutations (CSRF) | medium | mitigate | Inherited: SameSite=Lax session cookie + proxy Origin check for /api; server actions re-verify getCurrentUser and ownership per mutation (01-01 Task 4) |
| T-01-02-07 | Information Disclosure | search query reflection | low | mitigate | q is rendered as plain text within copy strings (React escapes it); maxLength 100 + server-side length guard + redirect on empty (RESEARCH Pitfall 6) |
| T-01-02-SC | Tampering | npm installs (react-markdown, remark-gfm) | high | mitigate | Both Approved in RESEARCH Package Legitimacy Audit (no postinstall scripts, official remarkjs repos); pinned exact versions in T-01-02-01; no other registry additions (UI-SPEC Registry Safety) |
</threat_model>

<verification>
1. `npm run test` — markdown XSS regression test + all 01-01 suites green
2. `npm run build` + `npx tsc --noEmit` + `npm run lint` — green
3. `npm run seed` — 5 posts / 4 categories / 6-8 tags / post_tags counts; idempotent second run; size < 200 MB
4. Grep gates: `grep -rn "rehype-raw\|dangerouslySetInnerHTML" app components lib` → 0; `grep -rn "post-form" app components` → 0; `grep -rnE "\bpublished\b" app components scripts lib __tests__` → 0 (word boundary; published_at exempt)
5. Guest runtime checks (`npm run dev` + seeded DB): GET /blog → 200 (no cookie); GET /blog/<draft-slug> → 404; GET /admin → redirect to /login; search "template" returns seeded hits; editor preview and /blog/[slug] render identically for a seeded post (Visual Quality Bar #1)
6. Dark mode spot check of preview pane, chips, placeholders (UI-SPEC backstop)
</verification>

<success_criteria>
Mapping to ROADMAP Phase 1 success criteria:

| Success criterion | Delivered by |
|-------------------|--------------|
| 1. Create/edit/delete posts with markdown editor + live preview | T-01-02-01 (pipeline) + T-01-02-03 (editor-shell, preview, wiring) |
| 2. Draft/publish workflow — only published on the public blog | T-01-02-02/T-01-02-03 (WHERE status = 'published' on every public query; drafts 404) |
| 3. Filter by category/tag + ILIKE search | T-01-02-02 (category/tag pages + /blog/search with escapeLike) |
| 4. Admin dashboard with edit/delete + category/tag management | T-01-02-03 (posts-table) + T-01-02-04 (admin categories/tags pages) |
| 5. 3-5 realistic demo posts + Demo Guide in README | T-01-02-04 (seed v2: 5 posts + README Demo Guide) |

Requirement coverage: CMS-01 (editor + actions wiring), CMS-02 (public status filtering), CMS-03 (admin taxonomy UI), CMS-04 (cover-upload client flow), CMS-05 (search page), CMS-06 (/blog grid), CMS-07 (/blog/[slug] article), CMS-08 (category/tag filter pages).
</success_criteria>

<output>
Create `.planning/phases/01-cms-app/01-02-SUMMARY.md` when done (per the summary template in execution_context).
</output>
