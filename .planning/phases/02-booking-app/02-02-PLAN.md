---
phase: 02-booking-app
plan: 02
plan_name: "UI pages — services listing, /book slot calendar flow, booking confirmation, admin bookings management"
status: draft
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/site.ts
  - components.json
  - components/ui/checkbox.tsx
  - app/(main)/services/page.tsx
  - app/(main)/services/loading.tsx
  - app/(main)/services/error.tsx
  - components/booking/service-card.tsx
  - app/(main)/book/page.tsx
  - app/(main)/book/loading.tsx
  - app/(main)/book/error.tsx
  - components/booking/slot-picker.tsx
  - components/booking/booking-dialog.tsx
  - app/(main)/booking/[id]/page.tsx
  - app/(main)/booking/[id]/loading.tsx
  - app/(main)/booking/[id]/error.tsx
  - app/(main)/booking/[id]/not-found.tsx
  - components/booking/booking-confirmation.tsx
  - app/admin/bookings/page.tsx
  - app/admin/bookings/loading.tsx
  - app/admin/bookings/error.tsx
  - components/admin/booking-filters.tsx
  - components/admin/bookings-table.tsx
  - components/layout/admin-shell.tsx
  - app/admin/page.tsx
autonomous: false
requirements: [BOOK-01, BOOK-02, BOOK-03, BOOK-05, BOOK-06, BOOK-07, BOOK-08]
must_haves:
  truths:
    - "BOOK-01: /services (public, (main) shell) lists the 3 seeded barber services as radio-style cards (name, description, mono '$30 · 30 min' price·duration via formatUsd, 'Book this' → /book?service={slug}); kicker 'The Barbershop' + H1 'Services' + description 'Three cuts, fourteen days of open slots.'; empty → 'No services yet' empty-state (Scissors)"
    - "BOOK-02: /book renders the rolling 14-day slot calendar (slot_date >= CURRENT_DATE AND <= CURRENT_DATE + 13) via LEFT JOIN bookings WHERE status <> 'cancelled' — a slot renders taken (disabled pill, title 'Already booked', aria-label '{time} — taken') exactly when a non-cancelled booking exists; days grouped server-side into days[] {date, isToday, slots[{id, time, taken}]}, zero-slot days omitted, 'Today' tag on the current date (UI-SPEC Interaction 10)"
    - "BOOK-03: single-page flow — service radiogroup (3 service-cards, grid gap-4 md:grid-cols-3, ?service={slug} preselect) → slot-picker → sticky confirm bar (mono service · date · time summary + 'Confirm booking' primary, disabled until both chosen) → booking-dialog (max-w-md): title 'Confirm your booking', summary rows Service/Date & time/Price (divide-y, mono values via formatSlotDate + formatSlotTime), footer 'Not yet' (secondary) + 'Confirm booking' (primary, Loader2 while pending); success → toast 'Booking confirmed.' → router.push('/booking/{id}'); conflict → destructive Alert 'That slot was just taken.' / 'Pick another time — the available slots have refreshed.' then dialog closes + router.refresh(); deposit failure → destructive Alert 'Payment failed. No charge was made. Try again.' with dialog staying open"
    - "BOOK-05: /admin/bookings (AdminShell) — page-header 'Bookings' with filtered count line ('N bookings · soonest first', pluralized '1 booking'), GET-form filters (Status all/pending/confirmed/cancelled + Service all/{slug}, auto-submit on change, 'Clear filters' ghost link only when a filter is active), table Service (+ mono price suffix) / Customer (name + email, max-w-[240px] truncate with title) / Date & time (mono, whitespace-nowrap) / Status badge (confirmed=default, pending=secondary, cancelled=outline+muted) / Actions; 'Confirm booking' (secondary, pending rows only) via dialog 'Confirm booking?' / 'The customer will receive a confirmation notice.'; 'Cancel booking' (ghost destructive, pending/confirmed rows) via alert-dialog 'Cancel booking?' / 'The slot reopens for others. This action cannot be undone.'; both → {ok} → toast + router.refresh(); rows sorted soonest-first"
    - "BOOK-06/BOOK-07: /booking/[id] (public, shareable) shows the summary card (Service/Date & time/Price/Deposit 'Paid {amount}' or '—'/Status — divide-y rows) plus 'Notices sent' section with email row (Mail, 'Booking confirmation', To {email} · {time}, body muted) and SMS row (MessageSquare, 'Reminder', To {phone} · {time}) queried by booking_id, honesty note 'Simulated — no real email or SMS was sent.', section omitted when zero notices; existing /admin/emails + /admin/sms render the same booking-linked rows (no new notification UI — CONTEXT locked)"
    - "BOOK-08: deposit checkbox in the confirm dialog, default unchecked, label 'Pay 25% deposit ({formatUsd(depositCents(price))}) to hold your spot — refundable if you cancel.' (checked state in primary); cancellation toasts carry the deposit suffix — 'Booking cancelled.' without deposit, 'Booking cancelled. Deposit refunded.' with one (single toast, no second event)"
    - "Owner actions: /booking/[id] renders for guests (shareable history) but 'Cancel booking' (ghost destructive) shows only when isOwner && status in (pending, confirmed) && slot start in future (slot_date/slot_time compared server-side against toDateKey(now)/current HH:MM); cancel goes through alert-dialog (same copy as admin) → toast per status → router.refresh() → cancelled badge shows, button disappears"
    - "Session gating (covered edge): guest selecting an available slot pill → router.push('/login?next=/book') (availability still renders); /admin/bookings protected by proxy + admin layout guard + per-action getCurrentUser re-check; non-owned mutation attempts get generic 404/message copy"
    - "Empty states (covered edge): /services 'No services yet' (no CTA); /book no-service-selected shows muted step hint 'Choose a service above to see open slots.' (not an empty-state); service with zero slots → 'No slots available' + CTA 'Browse services'; admin 'No bookings yet' (no CTA) vs filter mismatch 'No bookings match these filters.' + CTA 'Clear filters'"
    - "Loading/error states (covered edge): every route has loading.tsx (skeletons mirroring finals — services 3 card blocks, /book service cards + 3 date groups, confirmation summary rows, admin filter bar + table rows, with aria-label) and error.tsx (error-state + 'Try again'); dialog submit disabled + Loader2 while pending; stale/unknown booking id → styled not-found() 'Page not found' + 'Back to services' (primary) + 'Back to home' (ghost)"
    - "Accessibility + keyboard + overflow + long-text + reduced-motion (covered edges): service radiogroup with role='radio'/aria-checked + arrow-key navigation; pills are buttons in tab order with aria-labels ('{time} — available' / '{time} — taken'), selected aria-pressed, taken disabled; Radix dialog/alert-dialog focus traps; GET form keyboard-submittable; admin table overflow-x-auto + min-w-[720px]; pills flex-wrap (never scroll); card descriptions line-clamp-2; emails truncate with title attr; motion-reduce:transition-none; badge text is the full status word, never color-only"
  artifacts:
    - app/(main)/services/{page,loading,error}.tsx + components/booking/service-card.tsx
    - app/(main)/book/{page,loading,error}.tsx + components/booking/slot-picker.tsx
    - components/booking/booking-dialog.tsx + app/(main)/booking/[id]/{page,loading,error,not-found}.tsx + components/booking/booking-confirmation.tsx
    - components/ui/checkbox.tsx (shadcn CLI) + components.json (updated)
    - app/admin/bookings/{page,loading,error}.tsx + components/admin/{booking-filters,bookings-table}.tsx
    - components/layout/admin-shell.tsx (Bookings group), app/admin/page.tsx (Bookings stat-card), lib/site.ts (Services nav item)
  key_links:
    - createBooking/cancelBooking (02-01) bound via useActionState in booking-dialog/booking-confirmation; confirmBooking/cancelBookingAdmin bound per-row in bookings-table — the {ok} + bookingId returns drive toasts and navigation (no redirect() in actions)
    - bookingSchema field names (slotId, deposit) are the exact hidden inputs booking-dialog submits — deposit checkbox submits 'on'/absent
    - The availability query shape (LEFT JOIN bookings WHERE status <> 'cancelled', window CURRENT_DATE..+13, to_char HH24:MI) is the single source of taken-state truth the slot-picker renders
    - formatSlotDate/formatSlotTime/formatUsd from lib/booking.ts (02-01) are used everywhere — no ad-hoc Intl calls (UI-SPEC lock)
  backstops:
    - statement: "All Phase 2 pages pass 4.5:1 contrast in .dark (taken pills, cancelled badges, notice rows, filter bar, sticky confirm bar)"
      verification: backstop
    - statement: "Slot pill states (available-idle / selected / taken) are distinguishable without color — selected has Check icon + primary fill, taken is disabled + muted, never line-through"
      verification: backstop
---

<objective>
Build every Phase 2 UI surface the 02-01 data layer serves: the public service listing (/services), the single-page booking flow with the rolling 14-day slot calendar (/book + slot-picker + booking-dialog), the shareable booking confirmation page (/booking/[id] with notice rows and owner cancel), and the admin bookings management section (/admin/bookings with filters + confirm/cancel table), plus the AdminShell group, admin stat card, and nav item.

Purpose: This plan converts the locked CONTEXT decisions and UI-SPEC contract into the user-visible demo: guests browse services and availability without login (BOOK-01/02), authenticated users book atomically through the dialog (BOOK-03), the confirmation page proves the mock email/SMS/payment story (BOOK-06/07/08), and admins manage the queue (BOOK-05). The 11 covered + 2 backstop UI Considerations from the UI-SPEC are lifted into must_haves — no silent drops.

Output: All four page groups live under their shells with loading/error/empty states, wired to the 02-01 actions with toasts, navigation, and the UI-SPEC copy contract.

## Artifacts this phase produces

**New files (pages):** `app/(main)/services/{page,loading,error}.tsx`, `app/(main)/book/{page,loading,error}.tsx`, `app/(main)/booking/[id]/{page,loading,error,not-found}.tsx`, `app/admin/bookings/{page,loading,error}.tsx`

**New files (components):** `components/booking/service-card.tsx`, `components/booking/slot-picker.tsx`, `components/booking/booking-dialog.tsx`, `components/booking/booking-confirmation.tsx`, `components/admin/booking-filters.tsx`, `components/admin/bookings-table.tsx`

**New files (shadcn CLI):** `components/ui/checkbox.tsx` (+ `components.json` updated; package.json gains @radix-ui/react-checkbox — the ONLY new package, gated by checkpoint)

**Modified files:** `lib/site.ts` (defaultNav gains `{ label: "Services", href: "/services" }` after Blog), `components/layout/admin-shell.tsx` (Bookings group below Content), `app/admin/page.tsx` (Bookings stat-card)

**New functions/components exported:** `ServiceCard` (radio card), `SlotPicker` (14-day pill list), `BookingDialog` (confirm + deposit checkbox + conflict/payment alerts), `BookingConfirmation` (summary + notices + owner cancel), `BookingFilters` (GET form), `BookingsTable` (admin rows + per-row actions)

**New routes:** `/services`, `/book` (with `?service={slug}` preselect), `/booking/[id]`, `/admin/bookings` (all with loading/error; [id] also not-found)

**New env vars:** none

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-booking-app/02-CONTEXT.md
@.planning/phases/02-booking-app/02-RESEARCH.md
@.planning/phases/02-booking-app/02-PATTERNS.md
@.planning/phases/02-booking-app/02-UI-SPEC.md
@.planning/phases/02-booking-app/02-01-SUMMARY.md
@lib/site.ts
@components/layout/admin-shell.tsx
@app/admin/page.tsx

02-01-SUMMARY.md is genuinely needed: createBooking/cancelBooking/confirmBooking/cancelBookingAdmin signatures, FormState shapes, and bookingSchema field names are the contracts this plan binds to. The UI-SPEC governs everywhere it is not silent over CONTEXT (RESEARCH user_constraints). PATTERNS.md has the exact analog line references for every component below. Do NOT re-read files already covered by these reads while executing.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Public read path — /services listing + /book availability calendar (service-card + slot-picker + nav)</name>
  <files>
    lib/site.ts, app/(main)/services/page.tsx, app/(main)/services/loading.tsx, app/(main)/services/error.tsx,
    components/booking/service-card.tsx, app/(main)/book/page.tsx, app/(main)/book/loading.tsx,
    app/(main)/book/error.tsx, components/booking/slot-picker.tsx
  </files>
  <read_first>
    - app/(main)/blog/page.tsx (public RSC pattern: force-dynamic, single query, grid + EmptyState)
    - app/(main)/blog/search/page.tsx (searchParams-as-Promise contract — the /book?service= pattern)
    - components/blog/blog-card.tsx (card component shape)
    - components/ui/page-header.tsx, empty-state.tsx, skeleton.tsx (primitives to compose)
    - lib/site.ts (defaultNav array — insert point after Blog; do NOT bump TEMPLATE_VERSION)
    - 02-UI-SPEC.md (Page 1 spec, Page 2 spec, Interaction 5, a11y 1-3, Copywriting Contract rows for services/flow)
    - 02-PATTERNS.md (services page, book page, slot-picker "No Analog Found" sections)
    - 02-01-SUMMARY.md (availability data shapes — services columns, slots columns)
  </read_first>
  <action>
    Wire the thinnest end-to-end public path: DB read → RSC page → client components, per CONTEXT "Slot Calendar UX" (locked) and UI-SPEC.

    1. lib/site.ts: insert `{ label: "Services", href: "/services" }` into defaultNav immediately after the Blog item (Home · Blog · Services · Posts · Admin). Leave TEMPLATE_VERSION untouched (additive app feature, not a template fork — PATTERNS note).

    2. app/(main)/services/page.tsx (public by construction — /services is NOT in the proxy matcher): export const dynamic = "force-dynamic"; single query `SELECT id, slug, name, description, price_cents, duration_min FROM services ORDER BY created_at ASC` (stable seed insertion order: Haircut, Beard Trim, Haircut + Beard — UI-SPEC Page 1 Data); map rows to ServiceCardProps; render kicker row (mono "The Barbershop", text-primary, tracking-[0.28em], border-b-2 border-primary) + hairline, H1 "Services" (Display role), muted description "Three cuts, fourteen days of open slots.", then `grid gap-6 md:grid-cols-2 lg:grid-cols-3` of ServiceCard; rows.length === 0 → EmptyState (Scissors icon, heading "No services yet", body "The barbershop hasn't added any services yet.", no CTA). loading.tsx: 3 card skeletons (h-40 animate-pulse bg-muted blocks, aria-label="Loading services"). error.tsx: error-state + "Try again" (copy app/(main)/blog/error.tsx).

    3. components/booking/service-card.tsx (client): props { service: {id, slug, name, description, priceCents, durationMin}, selected, onSelect, index }; whole card is a radio target: role="radio", aria-checked, onKeyDown arrow-key nav (left/right/up/down move focus through siblings per Phase 1 segmented-control pattern); name (Heading role), description (muted, line-clamp-2, title attr), mono price·duration row via formatUsd(priceCents) + `${durationMin} min` comma-joined ("$30 · 30 min"); selection indicator (radio circle → Check icon in primary when selected); selected → border-primary ring-1 ring-primary; "Book this" CTA button (CalendarCheck icon) → Link href={`/book?service=${slug}`}. Note: on /services the cards act as nav cards (CTA navigates); on /book they are the radiogroup (parent role="radiogroup" aria-label="Choose a service").

    4. app/(main)/book/page.tsx (public availability — booking requires login only at slot selection): export const dynamic = "force-dynamic"; searchParams: Promise<{ service?: string }> — read the slug (no redirect on invalid; treat as unselected); fetch all services + the availability query for the selected service exactly per RESEARCH Pattern 5: SELECT s.id, to_char(s.slot_time, 'HH24:MI') AS slot_time, s.slot_date::text AS slot_date, (b.id IS NOT NULL) AS taken FROM slots s LEFT JOIN bookings b ON b.slot_id = s.id AND b.status <> 'cancelled' WHERE s.service_id = ${serviceId} AND s.slot_date >= CURRENT_DATE AND s.slot_date <= CURRENT_DATE + 13 ORDER BY s.slot_date, s.slot_time (UI-SPEC Interaction 10 — cancelled bookings never block); group rows server-side into days[] { date: string, isToday: boolean, slots: [{id, time, taken}] } where isToday = date === toDateKey(new Date()) and zero-slot days are omitted entirely (documented exception); page renders: page-header (kicker "Booking", H1 "Book a slot", description "Pick a service, choose a day, grab a time."), step 1 radiogroup of ServiceCard (preselected from ?service= when the slug matches a service; onSelect clears the selected slot), step 2 SlotPicker, step 3 confirm bar (see below). States: no service selected → muted helper text "Choose a service above to see open slots." (NOT an empty-state — flow hint); service selected but days[] empty → EmptyState (Clock icon, "No slots available", "No open times in the next 14 days for this service.", CTA "Browse services" → /services). Confirm bar: shown only when a slot is selected — mono summary line (service name · formatSlotDate(date) · formatSlotTime(time)) + "Confirm booking" primary button; sticky bottom-0 on mobile (bg-background/95 backdrop-blur border-t border-border p-4, full-width button), inline right-aligned non-sticky on md+. Slot selection is pure client state — no fetch per click. loading.tsx: skeleton kicker/title lines + 3 service-card blocks + 3 date-group blocks (header line + 6 pill blocks). error.tsx: error-state + "Try again".

    5. components/booking/slot-picker.tsx (client): props { serviceSlug: string, selectedSlotId: string | null, onSelect: (slot: {id, time, date} | null) => void, guest: boolean }; renders one h2 date-group per day (dateline: weekday via formatSlotDate — weekday text-foreground, date text-muted-foreground, uppercase mono text-xs tracking-[0.12em]; "Today" tag in text-primary when isToday); `flex flex-wrap gap-2` of pill buttons (px-3 py-2 min-h-10, gap-2); pill states per UI-SPEC Color: available-idle (bg-card border border-input text-foreground, hover bg-secondary hover:border-primary/60), available-selected (bg-primary text-primary-foreground border-primary + Check icon 14px, aria-pressed="true"), taken (bg-muted text-muted-foreground border border-border, disabled, title="Already booked", aria-label="{time} — taken"); available pills aria-label="{time} — available"; time text font-mono text-sm via formatSlotTime; guest clicking an available pill → router.push("/login?next=/book") (availability stays visible — only selection is gated, UI-SPEC Interaction 5); onSelect(null) when the same pill is clicked again (toggle), and the parent clears selection on service change.

    Avoid: any month-grid/calendar library, client-side fetching, ad-hoc date formats (use only the lib/booking.ts helpers), line-through on taken pills, new npm packages.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      npm run lint
    </automated>
    <human-check>
      npm run dev: /services shows the 3 seeded cards with $30 · 30 min metas and working "Book this" links; /book with ?service=haircut shows the 14-day grouped list with taken pills from the sample bookings, "Today" tag, and the no-service helper hint on plain /book; clicking an available pill while logged out redirects to /login?next=/book; dark mode renders pills correctly.
    </human-check>
  </verify>
  <acceptance_criteria>
    - lib/site.ts defaultNav contains { label: "Services", href: "/services" } positioned between Blog and Posts; TEMPLATE_VERSION unchanged (source assertion)
    - app/(main)/services/page.tsx and app/(main)/book/page.tsx both start with export const dynamic = "force-dynamic" (source assertion)
    - The /book availability query joins bookings with b.status <> 'cancelled' and filters slot_date >= CURRENT_DATE AND <= CURRENT_DATE + 13 (source assertion)
    - components/booking/slot-picker.tsx renders taken pills with the disabled attribute + title="Already booked" and available pills with aria-label containing "available" (source assertion)
    - /services and /book have loading.tsx + error.tsx siblings with skeleton/error-state composition (source assertion)
    - npx tsc --noEmit and npm run lint pass; browser check confirms the listed flows
  </acceptance_criteria>
  <done>The public read path works end-to-end: guests browse services and a live 14-day availability calendar with correct taken states, deep-linked preselect, and login gating on selection. Committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify @radix-ui/react-checkbox legitimacy before install (SUS flag — research package audit)</name>
  <files>components.json, package.json (approval precedes any modification)</files>
  <read_first>
    - 02-RESEARCH.md "Package Legitimacy Audit" (SUS verdict + rationale for @radix-ui/react-checkbox)
    - 02-UI-SPEC.md "Registry Safety" (checkbox is the only CLI add; official registry)
  </read_first>
  <what-built>
    A go/no-go decision on the ONLY new package this phase: @radix-ui/react-checkbox, which `npx shadcn@latest add checkbox` will write into package.json. The research audit flagged it [SUS] solely on the "too-new" heuristic (1.3.11 published 2026-07-24) — no trust signal is missing: it is the official Radix UI primitives package (already wrapped by the project's existing `radix-ui` unified dependency), ~51.8M weekly downloads, no postinstall script, not deprecated, source github.com/radix-ui/primitives. Per protocol, [SUS] installs require a blocking human verification — this checkpoint is never auto-approvable.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@radix-ui/react-checkbox (or `npm view @radix-ui/react-checkbox` for repository, dist-tags, scripts, and weekly downloads).
    2. Confirm: repository is github.com/radix-ui/primitives, no postinstall script, no deprecated flag, latest version matches what shadcn will install.
    3. If you approve, reply "approved" — the executor then runs `npx shadcn@latest add checkbox` (known npm-11 EALLOWSCRIPTS quirk: if the CLI's internal install fails, `npm install @radix-ui/react-checkbox` first, then re-run the CLI; do not hand-edit the generated checkbox.tsx afterwards).
    4. If anything looks wrong, describe the concern instead — the install does not proceed.
  </how-to-verify>
  <acceptance_criteria>
    - A human has explicitly approved or rejected the install with a resume signal (approval is never inferred from silence or auto-advance settings)
    - The verdict and the reasoning (official repo, download volume, no postinstall) are recorded in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow `npx shadcn@latest add checkbox`, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Booking completion — checkbox install + booking-dialog + booking-confirmation + /booking/[id]</name>
  <files>
    components/ui/checkbox.tsx, components.json, components/booking/booking-dialog.tsx,
    app/(main)/booking/[id]/page.tsx, app/(main)/booking/[id]/loading.tsx,
    app/(main)/booking/[id]/error.tsx, app/(main)/booking/[id]/not-found.tsx,
    components/booking/booking-confirmation.tsx
  </files>
  <read_first>
    - 02-01-SUMMARY.md (createBooking/cancelBooking FormState contracts, bookingSchema field names, notice query shapes)
    - components/admin/category-dialog.tsx (the exact client-dialog-over-server-action pattern: useActionState, toast, deferred close)
    - components/admin/cms-category-table.tsx lines 46-108 (Radix AlertDialog requestSubmit intercept — required for the cancel alert-dialog)
    - app/admin/emails/page.tsx (notice-row list styling precedent for the Notices section)
    - 02-UI-SPEC.md (Page 3 spec, Interactions 4/6/8, Copywriting Contract confirmation rows, a11y 4-5, Visual Quality Bar 8)
  </read_first>
  <action>
    Complete the booking flow per UI-SPEC Page 3 + Interaction 4/6, binding the 02-01 actions.

    1. Run `npx shadcn@latest add checkbox` (Task 2 approved it). On EALLOWSCRIPTS failure: `npm install @radix-ui/react-checkbox` first, then re-run the CLI. After generation, do not modify components/ui/checkbox.tsx.

    2. components/booking/booking-dialog.tsx (client): props { open: boolean, onOpenChange: (o: boolean) => void, service: {name: string}, slot: {date: string, time: string}, price: number } (price = priceCents). Radix dialog max-w-md. Title "Confirm your booking". Summary card: divide-y divide-border rows — Service (label muted / value foreground), Date & time (formatSlotDate + formatSlotTime, mono), Price (formatUsd(price), mono). Deposit field: checkbox name="deposit" default unchecked (checked state in primary) + Label htmlFor "Pay 25% deposit ({formatUsd(depositCents(price))}) to hold your spot — refundable if you cancel.". Form wraps hidden slotId input (value slot.id) + checkbox, bound via useActionState(createBooking, null). Footer: "Not yet" (secondary, onOpenChange(false)) + "Confirm booking" (primary, disabled + Loader2 while pending). Effects: state?.ok → toast.success("Booking confirmed.") → router.push(`/booking/${state.bookingId}`); state?.message → destructive Alert role="alert" with the message — and when the message is the conflict copy, also close the dialog + router.refresh() + clear the parent's selected slot (via a callback or onOpenChange) so the refreshed list shows the new taken state (UI-SPEC Interaction 3 — conflict reads as "just taken", next step = refreshed list); state?.errors?.["slotId"|"deposit"] → inline text-destructive paragraphs. The conflict/payment copy strings come from the action's message — the dialog renders whatever FormState.message carries ("That slot was just taken." / "Pick another time — the available slots have refreshed." / "Payment failed. No charge was made. Try again.").

    3. app/(main)/booking/[id]/page.tsx (public, shareable — guests render, owner gets actions): export const dynamic = "force-dynamic"; params: Promise<{ id: string }>; const id = (await params).id; if (!isUuid(id)) notFound(); data via Promise.all per RESEARCH Code Example — booking row (b.id, b.status, b.price_cents, b.deposit_payment_id, b.created_at, b.user_id, s.slot_date::text, to_char(s.slot_time, 'HH24:MI') AS slot_time, sv.name AS service_name, u.name AS user_name, u.email AS user_email, mp.status AS payment_status, mp.amount AS payment_amount, with LEFT JOIN mock_payments), notices (SELECT recipient, subject, body, created_at FROM mock_emails WHERE booking_id = $1 ORDER BY created_at DESC), sms rows (same for mock_sms); any query empty → notFound(); compute server-side: isOwner = user?.id === row.user_id (getCurrentUser); slotInFuture = slot_date > toDateKey(new Date()) || (slot_date === toDateKey(new Date()) && slot_time > current HH:MM string); canCancel = isOwner && status in (pending, confirmed) && slotInFuture; pass {booking, notices, isOwner, canCancel} to BookingConfirmation. not-found.tsx: styled booking 404 — "Page not found" + primary "Back to services" (→ /services) + ghost "Back to home". loading.tsx: summary-card skeleton (6 hairline rows). error.tsx: error-state + "Try again".

    4. components/booking/booking-confirmation.tsx (client — the owner-cancel alert-dialog needs interactivity; receives plain serializable props): props { booking, notices: {emails: [...], sms: [...]}, isOwner, canCancel }. Kicker row: mono "Booking reference" (text-primary tracking-[0.28em] border-b-2 border-primary) + mono id bookingRef(booking.id) (text-muted-foreground) + hairline. H1 "Booking details" + status Badge beside it (confirmed=default, pending=secondary, cancelled=outline+text-muted-foreground). Description "Sent to {email} · {formatSlotDate(date)}". Summary card (divide-y rows): Service, Date & time (mono), Price (formatUsd), Deposit — "Paid {formatUsd(payment_amount)}" when deposit_payment_id present else "—", Status. "Notices sent" section: mono primary label + hairline rule + honesty note "Simulated — no real email or SMS was sent." (mono 10px muted); email row (Mail icon, label "Booking confirmation", mono To {recipient} · {time}, body muted); SMS row (MessageSquare icon, label "Reminder", mono To {recipient} · {time}, body muted); section omitted entirely when zero notices. Action row (isOwner && canCancel only): "Book another" (secondary → /book) + "Cancel booking" (ghost destructive) → alert-dialog "Cancel booking?" / "The slot reopens for others. This action cannot be undone." / destructive "Cancel booking" button with the requestSubmit intercept pattern, form bound to cancelBooking with hidden bookingId; success effect: toast with the deposit suffix — "Booking cancelled." or "Booking cancelled. Deposit refunded." (single toast — suffix only when booking.deposit_payment_id present) → router.refresh() → page shows the cancelled badge and the cancel button disappears (UI-SPEC Interaction 6). A cancelled booking still renders fully (shareable history).
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      npm run test
    </automated>
    <human-check>
      Full browser flow (logged in): /book → select service → select slot → confirm with deposit checked → toast + land on /booking/[id] with summary + 2 notice rows + "Simulated" note; confirm a second booking with deposit unchecked → "—" deposit row; open the same slot in a second tab and book it → second dialog shows the conflict Alert and the list refreshes; cancel the first booking → toast with "Deposit refunded." suffix, cancelled badge, cancel button gone, and the slot re-renders available on /book; unknown id → styled 404.
    </human-check>
  </verify>
  <acceptance_criteria>
    - components/ui/checkbox.tsx exists and components.json lists the checkbox component (source assertion)
    - components/booking/booking-dialog.tsx binds createBooking via useActionState, submits hidden slotId + deposit checkbox, and renders FormState.message in a destructive role="alert" Alert (source assertion)
    - app/(main)/booking/[id]/page.tsx starts with force-dynamic, guards with isUuid → notFound(), and computes canCancel = isOwner && status in (pending,confirmed) && slot in future server-side (source assertion)
    - components/booking/booking-confirmation.tsx renders the summary rows, the honesty note, notice rows only when present, and the owner cancel alert-dialog with the requestSubmit intercept (source assertion)
    - npx tsc --noEmit and npm run test pass; browser flow matches the human-check list
  </acceptance_criteria>
  <done>The booking completion flow works: dialog with deposit toggle, atomic conflict handling, shareable confirmation page with mock-notice transparency, and owner cancel with the deposit-refund toast. Committed.</done>
</task>

<task type="auto">
  <name>Task 4: Admin bookings — filters + table + confirm/cancel + AdminShell group + stat card</name>
  <files>
    app/admin/bookings/page.tsx, app/admin/bookings/loading.tsx, app/admin/bookings/error.tsx,
    components/admin/booking-filters.tsx, components/admin/bookings-table.tsx,
    components/layout/admin-shell.tsx, app/admin/page.tsx
  </files>
  <read_first>
    - 02-01-SUMMARY.md (confirmBooking/cancelBookingAdmin FormState contracts)
    - app/admin/categories/page.tsx (admin section page skeleton + GET filter pattern) and app/admin/emails/page.tsx (table markup + notice rows)
    - components/admin/cms-category-table.tsx (client table + per-row AlertDialog requestSubmit pattern, skeleton variant, EmptyState branch)
    - components/admin/category-dialog.tsx (non-destructive confirm dialog pattern for "Confirm booking")
    - components/blog/blog-search.tsx (GET form, no client fetch — the booking-filters model)
    - components/layout/admin-shell.tsx (adminNav groups — insert point) and app/admin/page.tsx (StatCard grid + Promise.all)
    - 02-UI-SPEC.md (Page 4 spec, Interactions 7/9, Copywriting Contract admin rows, a11y 7)
  </read_first>
  <action>
    Build the admin bookings section per UI-SPEC Page 4 + Interaction 7/9, binding the 02-01 admin actions.

    1. app/admin/bookings/page.tsx: export const dynamic = "force-dynamic"; getCurrentUser → redirect("/login?next=/admin/bookings") if absent (layout guard exists; page re-checks per convention); searchParams: Promise<{ status?: string; service?: string }> — status ∈ {all, pending, confirmed, cancelled}, service ∈ {all} ∪ slugs, both default "all" (validate values; anything else → "all"); query: SELECT b.id, b.status, b.price_cents, sv.name AS service_name, sv.slug AS service_slug, sv.price_cents AS service_price_cents, u.name AS user_name, u.email AS user_email, s.slot_date::text AS slot_date, to_char(s.slot_time, 'HH24:MI') AS slot_time, mp.status AS payment_status FROM bookings b JOIN slots s ON s.id = b.slot_id JOIN services sv ON sv.id = s.service_id JOIN users u ON u.id = b.user_id LEFT JOIN mock_payments mp ON mp.id = b.deposit_payment_id [WHERE b.status = $status | AND sv.slug = $service] ORDER BY s.slot_date ASC, s.slot_time ASC (soonest first — the admin's operative queue); twin COUNT(*) query for the filtered count; description line "Manage appointments — {n} booking(s) · soonest first." (pluralize: "1 booking"); render card with BookingFilters + BookingsTable; rowCount 0 → the EmptyState variant (no filters active → "No bookings yet" CalendarCheck no CTA; filters active → "No bookings match these filters." + CTA "Clear filters" → /admin/bookings). loading.tsx: filter-bar skeleton (2 select blocks) + 5 table-row skeletons. error.tsx: error-state + "Try again".

    2. components/admin/booking-filters.tsx (server): props { current: { status: string; service: string }, services: {slug, name}[] }; `<form method="GET" action="/admin/bookings">` with Label htmlFor pairs — Status select (All statuses / Pending / Confirmed / Cancelled, values all/pending/confirmed/cancelled) and Service select (All services / each service name, values all/{slug}); both select defaultValue from current and auto-submit via onChange → form.submit() (server-rendered results, no client fetch — Phase 1 search pattern); "Clear filters" ghost Link to /admin/bookings shown only when status !== "all" || service !== "all"; bar styling: bg-card border-b border-border px-4 py-3 flex flex-wrap gap-4, selects w-full sm:w-auto.

    3. components/admin/bookings-table.tsx (client): props { rows: BookingRow[], current: {status, service} } where BookingRow = {id, status, serviceName, servicePriceCents, userName, userEmail, slotDate, slotTime, paymentStatus | null}; table inside overflow-x-auto wrapper, min-w-[720px] w-full; columns: Service (name + mono formatUsd(servicePriceCents) suffix muted), Customer (name + email muted truncate max-w-[240px] with title attr carrying the full email), Date & time (mono formatSlotDate + " · " + formatSlotTime, whitespace-nowrap), Status (Badge per UI-SPEC mapping — confirmed=default, pending=secondary, cancelled=outline + text-muted-foreground), Actions; row hover bg-muted/50. Actions: "Confirm booking" (secondary) rendered only when status === "pending", opens a non-destructive dialog ("Confirm booking?" / "The customer will receive a confirmation notice." / primary "Confirm booking" submit) bound to confirmBooking via per-row useActionState with hidden bookingId; "Cancel booking" (ghost destructive) rendered when status in (pending, confirmed), opens alert-dialog ("Cancel booking?" / "The slot reopens for others. This action cannot be undone." / destructive "Cancel booking" with the requestSubmit intercept) bound to cancelBookingAdmin; both dialogs are Radix with focus traps (a11y 8); on state.ok → toast (confirm: "Booking confirmed."; cancel: "Booking cancelled." with "Deposit refunded." suffix when paymentStatus is present) + router.refresh(); state.message → destructive inline Alert. Skeleton row variant + EmptyState handled by the page (table receives rows only when non-empty).

    4. components/layout/admin-shell.tsx: add a third group below "Content" — { group: "Bookings", items: [{ label: "Bookings", href: "/admin/bookings", icon: <CalendarCheck className="h-4 w-4" aria-hidden="true" /> }] }, mirroring the existing group object shape exactly. app/admin/page.tsx: add a Bookings StatCard (CalendarClock icon) to the grid — extend the existing Promise.all with a total-bookings COUNT query (SELECT count(*) FROM bookings), label "Bookings", 6 cards total (grid md:grid-cols-3 fits).
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      npm run lint
    </automated>
    <human-check>
      /admin/bookings (as demo@example.com): table shows the 4 seeded bookings soonest-first with correct badges; Status filter Pending → 1 row; Service filter Haircut → 2 rows; Clear filters appears only when a filter is active; Confirm on the pending row → toast + row becomes confirmed; Cancel a confirmed row → toast (with refunded suffix for deposit rows) + row becomes cancelled + slot reopens on /book; overview /admin shows the Bookings card.
    </human-check>
  </verify>
  <acceptance_criteria>
    - app/admin/bookings/page.tsx starts with force-dynamic, applies GET searchParams filters (status/service, both default all) in the SQL WHERE clause, orders by slot start ASC, and renders the filtered-count description line (source assertion)
    - components/admin/bookings-table.tsx shows "Confirm booking" only on pending rows and "Cancel booking" only on pending/confirmed rows, both via per-row useActionState-bound dialogs with the requestSubmit intercept (source assertion)
    - components/layout/admin-shell.tsx contains a "Bookings" group with a CalendarCheck icon item; app/admin/page.tsx contains a Bookings StatCard fed by a bookings COUNT (source assertions)
    - npx tsc --noEmit and npm run lint pass; browser admin flow matches the human-check list
  </acceptance_criteria>
  <done>Admins manage the full booking queue: filtered server-rendered table, confirm/cancel with toasts and race-safe reopen, plus the shell group and overview card. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → public pages (/services, /book, /booking/[id]) | Untrusted visitors read service + availability data; only slot selection triggers auth redirect |
| admin browser → /admin/bookings | Proxy + layout guard + per-action re-check gate admin mutations |
| client components → server actions | Dialog/table submit FormData (slotId, deposit, bookingId) to the 02-01 actions — client-side state is never trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | booking-dialog FormData (deposit flag, slotId) | medium | mitigate | bookingSchema (z.uuid + preprocess 'on' → boolean) re-validates server-side at action time; UI state is cosmetic — the action is the truth (ASVS 1: client-side validation never trusted) |
| T-02-10 | Spoofing | Guest bypassing the login gate by direct POST to createBooking | medium | mitigate | getCurrentUser() re-check inside the action (proxy only matches /admin — actions are the enforcement point for /book); UI gate is UX, not security (ASVS 1) |
| T-02-11 | Information Disclosure | XSS via notice bodies (email/SMS rows) on /booking/[id] | medium | mitigate | React escapes all text by default; no dangerouslySetInnerHTML anywhere; notice bodies are mock/seed-controlled strings (UI-SPEC honesty contract) |
| T-02-12 | Information Disclosure | Non-admin confirming/cancelling bookings | high | mitigate | /admin/bookings sits under the proxy matcher + app/admin/layout.tsx guard; both admin actions re-check getCurrentUser and redirect; generic "This booking no longer exists." on rowCount 0 (no enumeration) |
| T-02-13 | Tampering | Per-row action replay (double submit of confirm/cancel) | low | mitigate | Radix dialog pending state disables submit buttons (Loader2); actions are idempotent in effect — second UPDATE hits rowCount 0 on an already-final status and returns the generic message |
| T-02-14 | Information Disclosure | Slot/booking data of other users via /booking/[id] | low | accept | Public shareable confirmation is a locked product decision (CONTEXT); exposure is limited to one booking's summary + its own notices; UUID ids unguessable at demo scale |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | The ONLY install is `npx shadcn@latest add checkbox` (official registry) pulling @radix-ui/react-checkbox ([SUS] — research audit) — blocked behind Task 2's checkpoint:human-verify gate which is never auto-approvable (T-02-SC carry-over from research protocol) |

Severity per impact × likelihood at demo scale; blocking threshold: high (T-02-12 mitigated; T-02-SC gated).
</threat_model>

<verification>
- npx tsc --noEmit && npm run lint — no type/lint regressions across all 4 route groups + components
- npm run test — no regression in unit suites (helpers/schemas/mocks from 02-01)
- Browser verification of the full booking loop (guest browse → login → book with deposit → confirmation + notices → admin confirm → cancel with reopen + refund toast) — the human-check lists in Tasks 1/3/4 are the acceptance walk
- Backstop audits (per UI-SPEC ## UI Considerations): contrast ≥ 4.5:1 for taken pills / cancelled badges / notice rows in .dark; slot pill states distinguishable without color (selected = Check + primary, taken = disabled + muted)
- Empty/loading/error states present on all four routes per the lifted must_haves truths
</verification>

<success_criteria>
- BOOK-01..BOOK-08 all reachable through the UI: /services listing, /book calendar with taken states, dialog booking with deposit, confirmation page with mock email/SMS notices, admin confirm/cancel with filters, reopened slots visible to everyone
- UI-SPEC copy contract honored verbatim (conflict copy, payment-failure copy, deposit label, toasts with deposit suffix, empty-state copy, "Simulated" honesty note)
- Locked formats used exclusively via lib/booking.ts helpers (no ad-hoc Intl)
- Zero new runtime dependencies beyond the checkpoint-approved @radix-ui/react-checkbox
- All 11 covered UI Considerations have working implementations; the 2 backstop checks are executed at verify time
- proxy.ts untouched; public-by-construction routes confirmed; /admin guarded as before
</success_criteria>

<output>
Create `.planning/phases/02-booking-app/02-02-SUMMARY.md` when done.
</output>
