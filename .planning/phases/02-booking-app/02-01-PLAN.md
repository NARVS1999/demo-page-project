---
phase: 02-booking-app
plan: 01
plan_name: "Schema + data layer — 003_booking migration, mock extensions (client/bookingId), lib/booking.ts helpers (TDD), bookingSchema, atomic createBooking + cancel/confirm transactions, seed"
status: draft
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/003_booking.sql
  - lib/validate.ts
  - lib/mock/payment.ts
  - lib/mock/email.ts
  - lib/mock/sms.ts
  - lib/booking.ts
  - __tests__/booking.test.ts
  - __tests__/validate.test.ts
  - __tests__/mock.test.ts
  - app/(main)/book/actions.ts
  - app/admin/bookings/actions.ts
  - scripts/seed.ts
autonomous: true
requirements: [BOOK-03, BOOK-04, BOOK-05, BOOK-06, BOOK-07, BOOK-08]
must_haves:
  truths:
    - "npm run seed applies 003_booking.sql idempotently (schema_migrations ledger gains 003_booking; a second run is a no-op): services, slots, bookings tables exist with the locked columns, and mock_emails/mock_sms carry a nullable booking_id column (CONTEXT: Booking Schema & Transaction Model; RESEARCH Pattern 1)"
    - "Two users cannot claim the same slot: createBooking runs the conditional UPDATE slots SET booked_at = now() WHERE id = $1 AND booked_at IS NULL inside withPool; rowCount 0 maps to the UI-SPEC conflict message 'That slot was just taken.' (never a 500); the partial unique index bookings_active_slot_idx (slot_id) WHERE status <> 'cancelled' is the belt-and-braces 23505 fallback (BOOK-04, CONTEXT-locked claim model)"
    - "The 25% deposit (Math.round(price_cents * 0.25), integer cents) is written INSIDE the booking transaction via payment.createPayment(..., client); a claim failure or ROLLBACK removes the payment row — no orphan payment, no claimed-without-deposit state (BOOK-08, RESEARCH Pitfall 3)"
    - "Mock email + SMS notices persist with booking_id after COMMIT (email.sendEmail({..., bookingId}), sms.sendSms({..., bookingId})) so /booking/[id] and the existing /admin/emails + /admin/sms pages show booking-linked rows (BOOK-06, BOOK-07)"
    - "bookingSchema (slotId: z.uuid(), deposit: z.preprocess(v => v === 'on', z.boolean()).default(false)) accepts checked 'on', accepts unchecked/missing (null) as false, and rejects non-UUID slotId (RESEARCH Pitfall 6); the existing mock.test.ts suite passes unchanged, proving the mock extensions are backwards compatible"
    - "lib/booking.ts pure helpers (bookingRef, depositCents, toDateKey, formatSlotDate, formatSlotTime, formatUsd) pass their unit tests, are client-safe (no 'server-only'), and pin the UI-SPEC-locked Intl formats ('Tue, Aug 4' / '9:30 AM')"
    - "Cancel paths (user cancelBooking with owner WHERE user_id + upcoming guard; admin cancelBookingAdmin without them; confirmBooking pending→confirmed) reopen a slot only when no OTHER active booking exists on it (NOT EXISTS guard), refund the deposit via payment.refund(id, client) when present, and return generic 'This booking no longer exists.' on rowCount 0 (RESEARCH Pattern 2 — race-safe cancel)"
    - "Seed: 3 barber services with exact prices/durations (Haircut $30/30, Beard Trim $20/20, Haircut + Beard $45/50), Tue–Sat 09:00–16:00 hourly slots for the next 14 days (ON CONFLICT DO NOTHING, explicit ::date/::time casts), 4 sample bookings (2 confirmed, 1 pending, 1 cancelled) re-pointed into the current window every run via subselect on (slug, date, time), linked mock payment/email/SMS rows with fixed ids, TABLES report includes services/slots/bookings, size gate stays < 200 MB (CONTEXT: Demo Data & Domain; RESEARCH Pattern 4)"
  artifacts:
    - db/migrations/003_booking.sql
    - lib/booking.ts + __tests__/booking.test.ts
    - lib/validate.ts (+bookingSchema, BookingInput) + extended __tests__/validate.test.ts
    - lib/mock/payment.ts / email.ts / sms.ts (optional client?/bookingId? params) + extended __tests__/mock.test.ts
    - app/(main)/book/actions.ts (createBooking, cancelBooking, BookingConflictError)
    - app/admin/bookings/actions.ts (confirmBooking, cancelBookingAdmin)
    - scripts/seed.ts (SERVICES, WEEKLY_TEMPLATE, SAMPLE_BOOKINGS, slot loop, TABLES report)
  key_links:
    - withPool's callback is the ONLY place pg-style client.query(text, $n params) is used — the neon sql tagged template is HTTP-only and must never appear inside a transaction (RESEARCH Pitfall 5)
    - payment.createPayment(..., client) client-branch is the exact deposit path createBooking depends on; payment.refund(id, client) is the cancel path — the mock services stay the single writers of their tables (RESEARCH Pattern 3)
    - bookingSchema field names (slotId, deposit) are the exact hidden-field names booking-dialog (plan 02-02) must submit; formatSlotDate/formatSlotTime/formatUsd are the format contract every 02-02 page imports
    - Seed's slot_id re-pointing subselects keep sample bookings inside the rolling window on every re-run (RESEARCH Pitfall 2); the cancelled booking's slot is marked booked_at = NULL, demoing the reopen rule
---

<objective>
Build the Phase 2 booking data layer and API surface that plan 02-02's UI binds to: the additive `003_booking.sql` migration (services, slots, bookings + `booking_id` on mock_emails/mock_sms), backwards-compatible mock service extensions (in-transaction payment client, booking-linked email/SMS), client-safe pure helpers (lib/booking.ts), the `bookingSchema` Zod contract, the atomic `createBooking` transaction (the phase's core double-booking prevention), the race-safe cancel/confirm transactions, and the idempotent rolling-window seed.

Purpose: Every UI page in 02-02 renders data this plan persists, and every mutation 02-02 triggers runs through actions this plan defines. The atomic claim (`UPDATE slots ... WHERE booked_at IS NULL` + rowcount) IS BOOK-04; the in-transaction deposit IS BOOK-08; the post-commit notices with booking_id ARE BOOK-06/07; the cancel/confirm transactions ARE BOOK-05. Getting the transaction discipline right here prevents the documented orphan/partial-state and race bugs (RESEARCH Pitfalls 3, 5, 7).

Output: Schema applied to Neon, tested helpers + schema, atomic booking/cancel/confirm actions, and a seed that keeps demo data fresh under a rolling 14-day window.

## Artifacts this phase produces

**New files:** `db/migrations/003_booking.sql`, `lib/booking.ts`, `__tests__/booking.test.ts`

**Modified files:** `lib/validate.ts`, `lib/mock/payment.ts`, `lib/mock/email.ts`, `lib/mock/sms.ts`, `__tests__/validate.test.ts`, `__tests__/mock.test.ts`, `app/(main)/book/actions.ts`, `app/admin/bookings/actions.ts`, `scripts/seed.ts`

**SQL tables:** `services` (id, slug, name, description, price_cents, duration_min, created_at), `slots` (id, service_id, slot_date, slot_time, booked_at, created_at + UNIQUE(service_id, slot_date, slot_time)), `bookings` (id, slot_id, user_id, status, price_cents, deposit_payment_id, created_at, updated_at)

**SQL columns added:** `mock_emails.booking_id` (nullable, FK → bookings ON DELETE SET NULL), `mock_sms.booking_id` (same)

**SQL indexes:** `slots_calendar_idx` (partial, booked_at IS NULL), `bookings_active_slot_idx` (partial unique, status <> 'cancelled'), `bookings_user_idx`, `bookings_status_idx`, `mock_emails_booking_idx`, `mock_sms_booking_idx`

**New functions:** `createBooking`, `cancelBooking`, `confirmBooking`, `cancelBookingAdmin` (server actions); `bookingRef`, `depositCents`, `toDateKey`, `formatSlotDate`, `formatSlotTime`, `formatUsd` (lib/booking.ts pure helpers); `bookingSchema` (zod); extended `createPayment`/`refund` (client param), `sendEmail`/`sendSms` (bookingId param)

**New env vars:** none (9-var contract unchanged)

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-booking-app/02-CONTEXT.md
@.planning/phases/02-booking-app/02-RESEARCH.md
@.planning/phases/02-booking-app/02-PATTERNS.md
@lib/db.ts
@lib/mock/payment.ts
@lib/mock/email.ts
@lib/mock/sms.ts
@lib/validate.ts
@db/migrations/002_cms.sql
@scripts/seed.ts
@__tests__/mock.test.ts

All four phase artifacts are read fully before starting (CONTEXT decisions are locked and non-negotiable; RESEARCH Patterns 1-5 + Code Examples are the authoritative shapes; PATTERNS.md has the exact analog line references). lib/db.ts's withPool is the transaction vehicle. Do NOT re-read files already covered by these reads while executing.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end atomic booking — 003_booking migration + mock extensions + bookingSchema + createBooking</name>
  <files>
    db/migrations/003_booking.sql, lib/validate.ts, lib/mock/payment.ts, lib/mock/email.ts, lib/mock/sms.ts,
    app/(main)/book/actions.ts, __tests__/validate.test.ts, __tests__/mock.test.ts
  </files>
  <read_first>
    - db/migrations/002_cms.sql (idempotency header, DROP-then-ADD CHECK pair, IF [NOT] EXISTS discipline, ";\n" termination)
    - lib/db.ts (withPool BEGIN/COMMIT/ROLLBACK contract — the callback uses pg-style client.query ONLY)
    - lib/mock/payment.ts, lib/mock/email.ts, lib/mock/sms.ts (current signatures to extend backwards-compatibly)
    - lib/validate.ts (preprocess conventions, lines 40-52)
    - 02-RESEARCH.md Patterns 1, 2, 3 + Code Example "Atomic booking action"
    - 02-PATTERNS.md (mock extension section, validate.ts section, actions sections)
    - __tests__/mock.test.ts (existing stub pattern — must keep passing unchanged)
  </read_first>
  <behavior>
    - Test 1 (mock client-branch): payment.createPayment({amount: 750, currency: "usd"}, fakeClient) with fakeClient = {query: vi.fn()} calls fakeClient.query exactly once with SQL containing "INSERT INTO mock_payments" and params [id, 750, "usd", "succeeded"], and does NOT call the module sql. Same for refund(id, fakeClient) → "UPDATE mock_payments" with status 'refunded'.
    - Test 2 (schema): bookingSchema.safeParse({slotId: uuid, deposit: "on"}).success === true with deposit === true; safeParse({slotId: uuid, deposit: null}) → deposit === false; safeParse({slotId: "not-a-uuid"}) → success false with slotId error.
  </behavior>
  <action>
    Per CONTEXT "Booking Schema & Transaction Model" (locked) and RESEARCH Patterns 1-3, wire the thinnest end-to-end booking path: schema exists → claim → deposit → insert → notices → {ok, bookingId}.

    1. Create db/migrations/003_booking.sql exactly per RESEARCH Pattern 1: header comment "003_booking.sql — Phase 2 booking schema (idempotent; applied by npm run seed)"; CREATE TABLE IF NOT EXISTS services (id uuid PK DEFAULT gen_random_uuid(), slug text NOT NULL UNIQUE, name text NOT NULL, description text NOT NULL, price_cents integer NOT NULL CHECK (price_cents > 0), duration_min integer NOT NULL CHECK (duration_min > 0), created_at timestamptz NOT NULL DEFAULT now()); CREATE TABLE IF NOT EXISTS slots (id uuid PK DEFAULT gen_random_uuid(), service_id uuid NOT NULL REFERENCES services(id) ON DELETE CASCADE, slot_date date NOT NULL, slot_time time NOT NULL, booked_at timestamptz, created_at timestamptz NOT NULL DEFAULT now(), CONSTRAINT slots_service_date_time_key UNIQUE (service_id, slot_date, slot_time)); CREATE INDEX IF NOT EXISTS slots_calendar_idx ON slots (service_id, slot_date) WHERE booked_at IS NULL; CREATE TABLE IF NOT EXISTS bookings (id uuid PK DEFAULT gen_random_uuid(), slot_id uuid NOT NULL REFERENCES slots(id) ON DELETE RESTRICT, user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE, status text NOT NULL DEFAULT 'pending', price_cents integer NOT NULL, deposit_payment_id uuid REFERENCES mock_payments(id) ON DELETE SET NULL, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()); the DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT bookings_status_check CHECK (status IN ('pending','confirmed','cancelled')) pair (Phase 1 lesson — ADD CONSTRAINT has no IF NOT EXISTS); CREATE UNIQUE INDEX IF NOT EXISTS bookings_active_slot_idx ON bookings (slot_id) WHERE status <> 'cancelled' (BOOK-04 belt-and-braces); CREATE INDEX IF NOT EXISTS bookings_user_idx ON bookings (user_id, created_at DESC); CREATE INDEX IF NOT EXISTS bookings_status_idx ON bookings (status, created_at DESC); ALTER TABLE mock_emails ADD COLUMN IF NOT EXISTS booking_id uuid REFERENCES bookings(id) ON DELETE SET NULL; ALTER TABLE mock_sms ADD COLUMN IF NOT EXISTS booking_id uuid REFERENCES bookings(id) ON DELETE SET NULL; CREATE INDEX IF NOT EXISTS mock_emails_booking_idx ON mock_emails (booking_id) WHERE booking_id IS NOT NULL; CREATE INDEX IF NOT EXISTS mock_sms_booking_idx ON mock_sms (booking_id) WHERE booking_id IS NOT NULL. Every statement ends with ';' + newline (seed runner splits on ";\n"). Do not modify 001_init.sql or 002_cms.sql.

    2. Extend lib/mock/payment.ts backwards-compatibly (RESEARCH Pattern 3): createPayment({amount, currency = "usd", fail = false}, client?: import("@neondatabase/serverless").PoolClient) — when client is passed, insert the mock_payments row via client.query("INSERT INTO mock_payments (id, amount, currency, status) VALUES ($1, $2, $3, $4)", [id, amount, currency, status]) instead of the sql tagged template; keep assertMockMode() and the return shape {id, status, amount, currency} identical. Same branch pattern for refund(id, client?) with "UPDATE mock_payments SET status = 'refunded' WHERE id = $1". Never call the sql tagged template when client is present.

    3. Extend lib/mock/email.ts: sendEmail({to, subject, text, bookingId}: {to, subject, text, bookingId?: string}) — INSERT gains the booking_id column: VALUES ($id, $to, $subject, $text, 'sent', ${bookingId ?? null}). Same for lib/mock/sms.ts: sendSms({to, message, bookingId?}) with 'delivered'. Keep assertMockMode() and return shapes. Leave lib/mock/index.ts (barrel) untouched.

    4. Add to lib/validate.ts: export const bookingSchema = z.object({ slotId: z.uuid("Select a time slot."), deposit: z.preprocess((v) => v === "on", z.boolean()).default(false) }) and export type BookingInput = z.infer<typeof bookingSchema>. Client-safe module stays (no env, no server-only).

    5. Create app/(main)/book/actions.ts with the Phase 1 action skeleton (PATTERNS.md "Server Action Skeleton"): "use server"; import { redirect } from "next/navigation", { revalidatePath } from "next/cache", { flattenError } from "zod", { withPool, sql } from "@/lib/db", { payment, email, sms } from "@/lib/mock", { getCurrentUser } from "@/lib/session", { bookingSchema } from "@/lib/validate", { isUuid } from "@/lib/utils"; type FormState = { errors?: Record<string, string[] | undefined>; message?: string; ok?: boolean; bookingId?: string }; class BookingConflictError extends Error {}. Implement createBooking(_prev: FormState | null, formData: FormData): Promise<FormState> per RESEARCH Code Example: getCurrentUser() → redirect("/login?next=/book") if absent; bookingSchema.safeParse({slotId: formData.get("slotId"), deposit: formData.get("deposit")}); !success → { errors: flattenError(parsed.error).fieldErrors }. Inside withPool: (1) SELECT s.service_id, sv.name, s.slot_date::text AS slot_date, to_char(s.slot_time, 'HH24:MI') AS slot_time, sv.price_cents FROM slots s JOIN services sv ON sv.id = s.service_id WHERE s.id = $1 → rowCount 0 → throw BookingConflictError; (2) claim: client.query("UPDATE slots SET booked_at = now() WHERE id = $1 AND booked_at IS NULL", [slotId]) → rowCount === 0 → throw BookingConflictError (CONTEXT-locked claim); (3) deposit (BOOK-08): if deposit → const pay = await payment.createPayment({ amount: Math.round(priceCents * 0.25), currency: "usd" }, client); depositPaymentId = pay.id — the 25% formula inline for now (Task 3 swaps it to depositCents); (4) client.query("INSERT INTO bookings (slot_id, user_id, status, price_cents, deposit_payment_id) VALUES ($1, $2, 'pending', $3, $4) RETURNING id", [slotId, user.id, priceCents, depositPaymentId]) → return id. After withPool returns (COMMIT done — mock notices never fail): email.sendEmail({to: user.email, subject: "Booking confirmation", text: `Your ${serviceName} booking is confirmed for ${slotDate} at ${slotTime}.`, bookingId}); sms.sendSms({to: "+15551234567", message: `Reminder: your ${serviceName} booking at ${slotTime} on ${slotDate}. Reply STOP to opt out.`, bookingId}). Catch: BookingConflictError → { message: "That slot was just taken." } (UI-SPEC conflict copy, never a 500); (error as {code?: string}).code === "23505" → same message (partial-index fallback); else rethrow. Success: revalidatePath("/book"); return { ok: true, bookingId } — the bookingId is what plan 02-02's dialog navigates with. Do NOT call redirect() on success (it discards the return value — template convention).

    6. Write the RED tests from <behavior> into __tests__/validate.test.ts (bookingSchema cases) and __tests__/mock.test.ts (fake-client branch cases — no vi.mock change needed: the fake client is passed directly) and make them GREEN. The existing mock.test.ts tests must pass unchanged (proof of backwards compatibility).

    Avoid: tagged sql templates inside withPool (Pitfall 5), any string-concatenated SQL params, running createPayment outside the transaction (Pitfall 3), exposing DB state in error messages.
  </action>
  <verify>
    <automated>
      npm run test           (new bookingSchema + fake-client branch tests pass; existing mock.test.ts unchanged and green)
      npx tsc --noEmit
      npm run seed           (003_booking.sql applied via ledger; second consecutive run exits 0 as a no-op — idempotency proof)
    </automated>
  </verify>
  <acceptance_criteria>
    - db/migrations/003_booking.sql contains all three CREATE TABLE IF NOT EXISTS statements (services, slots, bookings), the bookings_status_check DROP/ADD pair, bookings_active_slot_idx with predicate "WHERE status <> 'cancelled'", and the two ALTER TABLE ... ADD COLUMN IF NOT EXISTS booking_id statements (source assertion)
    - lib/mock/payment.ts createPayment/refund accept an optional second PoolClient param and branch on it; lib/mock/email.ts + sms.ts accept optional bookingId and include it in the INSERT as a nullable param
    - __tests__/mock.test.ts passes without edits to its existing cases
    - bookingSchema parses deposit "on" → true, null/absent → false, rejects non-UUID slotId
    - app/(main)/book/actions.ts exports createBooking returning {ok: true, bookingId} on success and {message: "That slot was just taken."} on rowCount 0 or 23505 (behavior assertion via test or code review)
    - npm run seed exits 0 twice consecutively with 003_booking in the ledger
  </acceptance_criteria>
  <done>The single atomic booking path works end-to-end: migration applies idempotently, the claim/deposit/insert transaction compiles and is unit-covered at its testable surfaces (mock client-branch, schema), notices carry booking_id, and the conflict copy is wired. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/booking.ts pure helpers + unit tests (TDD — RED/GREEN/REFACTOR)</name>
  <files>lib/booking.ts, __tests__/booking.test.ts</files>
  <read_first>
    - lib/blog.ts (client-safe module contract: NO "server-only", pure helpers, header comment style)
    - __tests__/blog.test.ts (vitest describe/it/expect structure, @/ alias import, no mocking)
    - 02-PATTERNS.md (lib/booking.ts section — locked Intl formats)
    - 02-UI-SPEC.md (Typography table rows for slot time / date headers / booking reference; "Date formatting (lock)" paragraph)
  </read_first>
  <behavior>
    - Test 1 (bookingRef): bookingRef("1042abcd-1234-4111-8111-111111111111") === "#BK-1042" (uppercase first 4 uuid chars, mono display-only)
    - Test 2 (depositCents): depositCents(3000) === 750; depositCents(4500) === 1125; depositCents(1999) === 500 (Math.round)
    - Test 3 (toDateKey): toDateKey(new Date(2026, 7, 4)) === "2026-08-04" (zero-padded); toDateKey(new Date(2026, 11, 31)) === "2026-12-31"
    - Test 4 (formatSlotDate): formatSlotDate("2026-08-04") === "Tue, Aug 4" via Intl.DateTimeFormat("en-US", { weekday: "short", month: "short", day: "numeric" })
    - Test 5 (formatSlotTime): formatSlotTime("09:00") === "9:00 AM"; formatSlotTime("16:00") === "4:00 PM"; formatSlotTime("12:30") === "12:30 PM"; formatSlotTime("00:15") === "12:15 AM" via Intl.DateTimeFormat("en-US", { hour: "numeric", minute: "2-digit" })
    - Test 6 (formatUsd): formatUsd(3000) === "$30"; formatUsd(750) === "$7.50"; formatUsd(25) === "$0.25" (cents → dollars, trailing ".00" trimmed to match UI-SPEC "$30" copy)
  </behavior>
  <action>
    Create lib/booking.ts as a client-safe pure-helpers module (mirror lib/blog.ts's header + contract: NO "server-only" — imported by browser components, server actions, and tests). Implement exactly five locked helpers plus one display helper:

    1. bookingRef(id: string): string — "#BK-" + id.slice(0, 4).toUpperCase(). Display-only ref; never stored (RESEARCH Anti-Pattern "Deriving the booking ref from a DB sequence").
    2. depositCents(priceCents: number): number — Math.round(priceCents * 0.25). The single source of truth for the 25% deposit (UI-SPEC Interaction 4).
    3. toDateKey(d: Date): string — local YYYY-MM-DD with padStart(2, "0") on month and day (RESEARCH Pattern 4 helper; used by seed and by the server-side isToday computation in plan 02-02).
    4. formatSlotDate(dateKey: string): string — parse "YYYY-MM-DD" into a local Date (new Date(y, m-1, d), never new Date(str) — UTC parsing pitfall), then format with Intl.DateTimeFormat("en-US", { weekday: "short", month: "short", day: "numeric" }) → "Tue, Aug 4".
    5. formatSlotTime(time: string): string — parse "HH:MM" into a local Date (hours/minutes), format with Intl.DateTimeFormat("en-US", { hour: "numeric", minute: "2-digit" }) → "9:00 AM" / "4:00 PM".
    6. formatUsd(cents: number): string — "$" + (cents / 100).toFixed(2), then strip a trailing ".00" (so 3000 → "$30", 750 → "$7.50", 25 → "$0.25") — matches UI-SPEC copy ("$30 · 30 min", "Paid {amount}").

    One helper per format — no ad-hoc Intl calls anywhere else in the phase (UI-SPEC lock). TDD sequence: write __tests__/booking.test.ts first (plain vitest describe/it/expect per __tests__/blog.test.ts, importing via @/lib/booking), watch the tests fail (RED), implement the module until green (GREEN), then refactor for clarity keeping tests green (REFACTOR). Commit per gate: test({phase}-{plan}): add failing test for lib/booking helpers, feat({phase}-{plan}): implement lib/booking helpers, refactor({phase}-{plan}): clean up lib/booking helpers.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/booking.test.ts exists and covers all six helpers with the pinned I/O from <behavior> (source assertion: each helper has ≥ 1 expect case)
    - lib/booking.ts contains no "server-only" import and no Node-only APIs (client-safe — buildable into the browser bundle)
    - All six behavior cases pass in `npm run test`
  </acceptance_criteria>
  <done>All six helpers are unit-tested, client-safe, and pin the UI-SPEC-locked formats; committed with RED/GREEN/REFACTOR commits.</done>
</task>

<task type="auto">
  <name>Task 3: Seed (3 services + rolling 14-day slots + re-pointed sample bookings + linked notices/payments) + cancel/confirm transactions</name>
  <files>
    scripts/seed.ts, app/(main)/book/actions.ts, app/admin/bookings/actions.ts
  </files>
  <read_first>
    - scripts/seed.ts (full file: env bootstrap, migration runner, fixed-UUID data arrays, upsert loop patterns, TABLES report, 200 MB gate)
    - 02-RESEARCH.md Pattern 4 + Code Example "Cancel transaction (shared by user + admin actions)"
    - 02-PATTERNS.md (seed.ts section — fixed prefixes, subselect FK pattern, slot generation)
  </read_first>
  <action>
    Per CONTEXT "Demo Data & Domain" (locked) and RESEARCH Pattern 4, extend scripts/seed.ts and add the shared cancel/confirm transactions.

    SEED — 1. Add const SERVICES = [3 rows] with fixed ids "e1111111-1111-4111-8111-111111111111" (slug "haircut", name "Haircut", description "Classic cut, hot towel finish, and style.", priceCents 3000, durationMin 30), "e2222222-2222-4222-8222-222222222222" (slug "beard-trim", name "Beard Trim", description "Shape, line-up, and beard oil.", priceCents 2000, durationMin 20), "e3333333-3333-4333-8333-333333333333" (slug "haircut-beard", name "Haircut + Beard", description "The full reset — cut and beard together.", priceCents 4500, durationMin 50). Upsert with sqlDirect: INSERT INTO services (id, slug, name, description, price_cents, duration_min) VALUES ($1..$6) ON CONFLICT (slug) DO UPDATE SET name = EXCLUDED.name, description = EXCLUDED.description, price_cents = EXCLUDED.price_cents, duration_min = EXCLUDED.duration_min (keyed on slug per CONTEXT).
    2. Add const WEEKLY_TEMPLATE: Record<number, string[]> = { 2: [09:00..16:00], 3: same, 4: same, 5: same, 6: same } — 8 hourly times per Tue–Sat day (RESEARCH Pattern 4 grid).
    3. Slot generation loop: for offset 0..13 — const day = new Date(); day.setDate(day.getDate() + offset); const times = WEEKLY_TEMPLATE[day.getDay()]; skip when absent; for each service × time: INSERT INTO slots (service_id, slot_date, slot_time) VALUES ($1, ${toDateKey(day)}::date, ${time}::time) ON CONFLICT (service_id, slot_date, slot_time) DO NOTHING, with toDateKey imported from "@/lib/booking" (JS-computed string param + explicit casts — RESEARCH Pitfall 8: never interpolate a date literal into the SQL string).
    4. Add const SAMPLE_BOOKINGS = [4 rows per RESEARCH Pattern 4]: {id "f1111111-1111-4111-8111-111111111111", serviceSlug "haircut", daysAhead 2, time "10:00", status "confirmed", deposit true}, {id "f2222222-2222-4222-8222-222222222222", serviceSlug "beard-trim", daysAhead 3, time "11:00", status "confirmed", deposit false}, {id "f3333333-3333-4333-8333-333333333333", serviceSlug "haircut", daysAhead 1, time "14:00", status "pending", deposit false}, {id "f4444444-4444-4444-8444-444444444444", serviceSlug "haircut-beard", daysAhead 4, time "15:00", status "cancelled", deposit true}. Upsert each: INSERT INTO bookings (id, slot_id, user_id, status, price_cents, deposit_payment_id) VALUES ($id, (SELECT id FROM slots WHERE service_id = (SELECT id FROM services WHERE slug = $slug) AND slot_date = ${toDateKey(addDays(now, daysAhead))}::date AND slot_time = $time::time), (SELECT id FROM users WHERE email = 'demo@example.com'), $status, $priceCents, $payId) ON CONFLICT (id) DO UPDATE SET slot_id = EXCLUDED.slot_id, status = EXCLUDED.status, price_cents = EXCLUDED.price_cents, deposit_payment_id = EXCLUDED.deposit_payment_id, updated_at = now() — the subselect re-points each booking into the CURRENT window on every run (RESEARCH Pitfall 2; demo user is "demo@example.com" / DEMO_USER_ID from the existing users upsert). Then booked_at marks: status <> 'cancelled' → UPDATE slots SET booked_at = now() WHERE id = $slotId; the cancelled one → UPDATE slots SET booked_at = NULL WHERE id = $slotId (reopen rule demo — UI-SPEC Visual Quality Bar 7).
    5. Deposit payments (fixed ids, e4 prefix): deposit bookings insert mock_payments rows with id "e4111111-1111-4111-8111-111111111111" (f1, status 'succeeded', amount 750) and "e4222222-2222-4222-8222-222222222222" (f4, status 'refunded', amount 1125); f4's payment references it as refunded (cancelled demo booking). Upsert ON CONFLICT (id) DO UPDATE. Use these ids as SAMPLE_BOOKINGS' deposit_payment_id values.
    6. Linked notices: extend the existing MOCK_EMAILS/MOCK_SMS arrays with booking_id-bearing entries — 2 emails (ids "a3111111-1111-4111-8111-111111111111" → booking f1, "a4111111-1111-4111-8111-111111111111" → booking f4, recipient demo@example.com, subject "Booking confirmation", realistic body text) and 2 sms rows (ids "b3111111-1111-4111-8111-111111111111" → f1, "b4111111-1111-4111-8111-111111111111" → f4, recipient "+15551234567", realistic reminder text). Existing entries keep booking_id null. Their INSERTs gain the booking_id column (nullable param).
    7. Add "services", "slots", "bookings" to the TABLES array (lines 289-300) so the size report covers them; the < 200 MB gate and exit codes stay untouched.

    ACTIONS — 8. app/(main)/book/actions.ts: add cancelBooking(_prev, formData) with the owner-scoped variant of the RESEARCH cancel transaction: getCurrentUser → redirect("/login?next=/book") if absent; const id = formData.get("bookingId"); typeof id !== "string" || !isUuid(id) → { message: "This booking no longer exists." } (generic, anti-enumeration); withPool: client.query("UPDATE bookings b SET status = 'cancelled', updated_at = now() FROM slots s WHERE b.id = $1 AND b.slot_id = s.id AND b.status IN ('pending', 'confirmed') AND b.user_id = $2 AND (s.slot_date > CURRENT_DATE OR (s.slot_date = CURRENT_DATE AND s.slot_time > CURRENT_TIME)) RETURNING b.slot_id, b.deposit_payment_id", [id, user.id]) — the owner WHERE + "upcoming" guard are the ONLY differences from the admin variant; rowCount 0 → return { message: "This booking no longer exists." }; race-safe reopen: client.query("UPDATE slots SET booked_at = NULL WHERE id = $1 AND booked_at IS NOT NULL AND NOT EXISTS (SELECT 1 FROM bookings WHERE slot_id = $1 AND status <> 'cancelled' AND id <> $2)", [slot_id, id]) (RESEARCH Pitfall 7 — never clear a concurrent fresh claim); if deposit_payment_id → await payment.refund(deposit_payment_id, client); revalidatePath("/book"); return { ok: true }.
    9. app/admin/bookings/actions.ts: "use server"; FormState skeleton per Phase 1 taxonomy actions (PATTERNS.md); both actions: getCurrentUser → redirect("/login?next=/admin/bookings") if absent; id isUuid guard → generic message. confirmBooking: withPool → client.query("UPDATE bookings b SET status = 'confirmed', updated_at = now() FROM slots s WHERE b.id = $1 AND b.slot_id = s.id AND b.status = 'pending' RETURNING b.user_id, s.slot_date::text, to_char(s.slot_time, 'HH24:MI')", [id]) → rowCount 0 → { message: "This booking no longer exists." }; post-COMMIT: SELECT email FROM users WHERE id = $userId (sql tagged template — post-transaction is fine), then email.sendEmail({to, subject: "Booking confirmed", text: `Your ${serviceName} booking at ${slotDate} ${slotTime} is confirmed.`, bookingId: id}) with serviceName from a post-commit SELECT JOIN or RETURNING extension — keep it one extra query. cancelBookingAdmin: same cancel transaction as step 8 WITHOUT the owner WHERE and WITHOUT the upcoming guard (admin cancels any pending/confirmed booking, past or future); revalidatePath("/admin/bookings") + revalidatePath("/book") in both; return { ok: true }.
    10. Refactor: in createBooking (Task 1), replace the inline Math.round(priceCents * 0.25) with depositCents(priceCents) imported from "@/lib/booking" — single source of truth; run the full test suite to confirm no regression.

    Avoid: unconditional booked_at = NULL on cancel (Pitfall 7), interpolating date literals into SQL strings (Pitfall 8), reusing c/d/a/b uuid prefixes (PATTERNS fixed-prefix rule), touching the migration runner or 200 MB gate logic.
  </action>
  <verify>
    <automated>
      npm run seed           (exit 0; services/slots/bookings upserted; size report includes 3 new tables)
      npm run seed           (second consecutive run exit 0 — sample bookings re-pointed, no duplicate errors)
      npx tsc --noEmit
      npm run test
    </automated>
    <human-check>
      In the browser at /book (after plan 02-02 lands) the seeded calendar shows taken pills from the 2 confirmed + 1 pending sample bookings, and the cancelled booking's slot renders available — but this visual check is not required to close this task; the seed's booked_at state is verifiable via SQL: SELECT count(*) FROM bookings WHERE status <> 'cancelled' returns 3, and the f4 slot's booked_at is NULL.
    </human-check>
  </verify>
  <acceptance_criteria>
    - scripts/seed.ts contains SERVICES (3 rows with the exact slugs/prices/durations), WEEKLY_TEMPLATE (Tue–Sat, 8 hourly times), a 0..13 slot-generation loop with toDateKey + explicit ::date/::time casts, SAMPLE_BOOKINGS (4 rows, ids f1-f4, mixed statuses), booked_at marking (cancelled → NULL), payment rows e4111111/e4222222 (succeeded/refunded), notice rows a3111111/a4111111 + b3111111/b4111111 carrying booking_id f1/f4, and "services", "slots", "bookings" in the TABLES array (source assertions)
    - app/(main)/book/actions.ts exports cancelBooking with the owner WHERE user_id + upcoming CURRENT_DATE/CURRENT_TIME guard and the NOT EXISTS reopen guard (source assertion)
    - app/admin/bookings/actions.ts exports confirmBooking (pending→confirmed + post-commit email notice with bookingId) and cancelBookingAdmin (no owner guard, race-safe reopen, refund) (source assertion)
    - npm run seed exits 0 on two consecutive runs; the second run reports < 200 MB
    - npx tsc --noEmit and npm run test both pass
    - createBooking uses depositCents(priceCents) (no inline 25% math remains — grep for "Math.round(priceCents" finds none in app/)
  </acceptance_criteria>
  <done>Seed populates a live rolling calendar with realistic demo data; user/admin cancel and admin confirm work atomically with race-safe reopen, refunds, and generic error copy. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → server action | Untrusted FormData (slotId, deposit) crosses into createBooking/cancelBooking/confirmBooking/cancelBookingAdmin |
| server action → Neon (withPool) | Trusted app code issues pg-style parameterized statements inside a transaction |
| mock services → mock_* tables | Mock writers (extended with client/bookingId params) persist events; booking_id links notices to bookings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | createBooking atomic claim (`UPDATE slots ... WHERE booked_at IS NULL`) | high | mitigate | One atomic conditional UPDATE inside withPool; rowCount 0 → "That slot was just taken."; partial unique index bookings_active_slot_idx (WHERE status <> 'cancelled') catches the 23505 race as belt-and-braces (BOOK-04; ASVS 1: race condition handling) |
| T-02-02 | Spoofing | createBooking without a session | high | mitigate | getCurrentUser() re-check at the top of every action + redirect("/login?next=...") — proxy.ts only guards /admin, so actions are the enforcement point for /book (CONTEXT: login required to book) |
| T-02-03 | Elevation | confirmBooking/cancelBookingAdmin invoked by non-admin | high | mitigate | /admin route group protected by proxy.ts matcher + app/admin/layout.tsx guard; actions re-check getCurrentUser() and redirect (defense in depth; ASVS 1: admin functions require admin session) |
| T-02-04 | Information Disclosure | cancel of another user's booking; id probing via /booking/[id] and actions | medium | mitigate | Owner-scoped `AND b.user_id = $2` on user cancel; isUuid guards before SQL; generic "This booking no longer exists." on rowCount 0; notFound() on unknown ids — no DB-state enumeration (Phase 1 pattern) |
| T-02-05 | Tampering | Cancel/reopen race wiping a concurrent fresh claim | medium | mitigate | Reopen guarded with NOT EXISTS (any active booking on the slot other than the one being cancelled); UI-SPEC conflict flow absorbs the rare spurious race (RESEARCH Pitfall 7) |
| T-02-06 | Tampering | SQL injection via string-concatenated params inside withPool | medium | mitigate | Hard rule: pg-style client.query(text, $n params) ONLY inside transactions; raw SQL only; no string-built SQL (RESEARCH Pitfall 5; AGENTS.md) |
| T-02-07 | Information Disclosure | Payment/booking data of other users | low | accept | Confirmation page is public-by-design (shareable); only the taken boolean and the booking's own notices are exposed; UUID ids are unguessable at demo scale |
| T-02-08 | Denial of Service | Slot flood (one user books every slot) | low | accept | Demo app at portfolio scale; admin cancel path exists; no rate limiting at $0 hosting scope (documented demo limitation) |
| T-02-SC | Tampering | npm/cargo/pip installs | high | mitigate | Zero new npm packages in this plan — mock extensions only touch existing files; the sole install of the phase (@radix-ui/react-checkbox via shadcn CLI, [SUS]) is gated by a blocking human-verify checkpoint in plan 02-02 (never auto-approvable) |

Severity per impact × likelihood at demo scale; blocking threshold: high (all high threats above are mitigated, none accepted).
</threat_model>

<verification>
- npm run seed twice — idempotent application of 003_booking.sql (ledger) + re-pointed sample bookings + size report < 200 MB with services/slots/bookings in TABLES
- npm run test — booking.test.ts (6 helpers), validate.test.ts (bookingSchema), mock.test.ts (fake-client branches + unchanged legacy cases) all green
- npx tsc --noEmit — no type errors across the new/modified files
- npm run lint — no new lint violations
- Code review spot-check (gsd-code-review or equivalent): the withPool callback contains only client.query(text, $n) — no sql tagged template, no string concatenation; createBooking catches BookingConflictError + 23505 before any generic handler
</verification>

<success_criteria>
- BOOK-04 double-booking prevention is implemented exactly as CONTEXT locked: conditional UPDATE + rowcount → conflict copy, partial unique index as fallback
- BOOK-08 deposit is atomic: createPayment(..., client) inside the transaction; refund(id, client) on every cancel path
- BOOK-06/BOOK-07 notices persist with booking_id after COMMIT — consumable by the existing /admin/emails + /admin/sms and the new confirmation page
- BOOK-03 createBooking returns {ok, bookingId} ready for the 02-02 dialog navigation; BOOK-05 confirm/cancel actions ready for the 02-02 admin table
- Seed is idempotent under a rolling window: sample bookings always re-point into the current 14 days; 3 services match the UI-SPEC pricing exactly
- The 6 lib/booking.ts helpers pin the UI-SPEC-locked formats; bookingSchema is the exact contract booking-dialog submits
- No new env vars; proxy.ts untouched; mock.test.ts legacy cases pass unchanged (backwards compatibility proven)
</success_criteria>

<output>
Create `.planning/phases/02-booking-app/02-01-SUMMARY.md` when done.
</output>
