# Deferred Items

## Existing lint warnings

`npm run lint` passes with warnings that predate Phase 3 and are outside the
backend ecommerce plan: unused test/error-boundary parameters and existing
`<img>` optimization/alt warnings in blog components. No new Phase 3 lint
errors or warnings were introduced.
