# Milestone v1.0: Free Fullstack Showcase

**Status:** SHIPPED 2026-08-03
**Phases:** 0-4
**Total Plans:** 9

## Overview

Build 10-30 fully deployed fullstack demo apps at $0/month. The foundation is a reusable `nextjs-starter` template with auth, database, mock services, and UI components. Three flagship apps (CMS, Booking, Ecommerce) validate the template and demonstrate range. A portfolio shell aggregates everything. Every project gets its own GitHub repo and Vercel deployment with a shared $0 budget.

## Phases

### Phase 0: Template Foundation

**Goal**: A battle-tested reusable template that every project inherits from — auth, database, mock services, seed script, and UI components.
**Depends on**: Nothing (first phase)
**Plans**: 2 plans

- [x] 00-01: Project scaffolding — Next.js 16 + TypeScript + Tailwind v4 + shadcn/ui + Neon driver + Zod validation
- [x] 00-02: Core infrastructure — auth system, DB pool, mock services, seed script, env validation, dark/light theme

### Phase 1: CMS App

**Goal**: First flagship project — blog/CMS with post CRUD, markdown editor, categories/tags, admin dashboard.
**Depends on**: Phase 0
**Plans**: 2 plans

- [x] 01-01: Schema + API routes — posts, categories, tags tables; CRUD route handlers; search endpoint
- [x] 01-02: UI pages — public blog list, single post, category/tag filters, admin dashboard, markdown editor

### Phase 2: Booking App

**Goal**: Service scheduling app with slot calendar, booking flow, double-booking prevention, mock email/SMS.
**Depends on**: Phase 0
**Plans**: 2 plans

- [x] 02-01: Schema + API routes — services, slots, bookings tables; atomic booking with BEGIN/COMMIT transaction
- [x] 02-02: UI pages — service listing, slot calendar, booking confirmation, admin management, mock email/SMS views

### Phase 3: Ecommerce App

**Goal**: Coffee shop storefront with product catalog, shopping cart, checkout with mock payment, admin orders.
**Depends on**: Phase 0
**Plans**: 2 plans

- [x] 03-01: Schema/API surface — shop categories, products, persistent cart, transactional checkout, order lifecycle, mock receipts, and seed
- [x] 03-02: UI surfaces — product catalog/detail, cart, checkout, owner confirmation, admin orders dashboard, and receipt links

### Phase 4: Portfolio Shell

**Goal**: The front door to the entire showcase — responsive project grid aggregating all deployed apps.
**Depends on**: Phase 1, Phase 2, Phase 3
**Plans**: 1 plan

- [x] 04-01: Project grid, metadata registry, responsive layout, project cards with links/badges/credentials

## Milestone Summary

**Key Decisions:**
- Next.js + Postgres only — $0 hosting on Vercel + Neon
- Mock services via lib/mock/* — swappable without touching business logic
- Separate repo per project — each gets own Vercel deployment
- Hand-rolled auth with jose + bcryptjs — no NextAuth, no Prisma, no Redis
- Neon cloud DB for local dev — no local Postgres install needed
- Atomic operations (booking/ecommerce) via conditional UPDATE in Pool transactions

**Issues Resolved:**
- Server actions defect: root-caused to curl artifact, not environment defect
- shadcn 4.16.1 preset flow: manual dep installs + CLI re-run pattern established
- npm 11.18 EALLOWSCRIPTS: documented pattern for scaffolding
- Booking date seeding: nthTemplateDay pattern for Tue-Sat grid alignment
- Ecommerce checkout: locked integer-cent snapshots + failed payment isolation

**Issues Deferred:**
- Rate limiting on auth endpoints — deferred for future app at demo scale
- Batch projects (7-27 additional apps) — v2 milestone

**Technical Debt Incurred:**
- None — all phases completed with passed verification

---

_For current project status, see .planning/ROADMAP.md_
