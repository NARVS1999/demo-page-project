---
phase: 00-template-foundation
plan: 00-01
subsystem: infra
tags: [nextjs, tailwind-v4, shadcn, vitest, typescript]

# Dependency graph
requires:
  - phase: "research (pre-planning)"
    provides: "verified commands, version pins, package legitimacy audit"
provides:
  - "Next.js 16.2.12 App Router scaffold with TypeScript, ESLint, Tailwind v4, Turbopack, @/* alias"
  - "shadcn/ui 4.16.1 with radix base + Nova preset, 19 UI-SPEC components, canonical oklch tokens"
  - "vitest 4.1.10 harness (jsdom + RTL + @/ alias + server-only stub) with green smoke test"
  - ".env.example with the 9 canonical env vars (dual Neon URLs, SESSION_SECRET, 6 MOCK_* switches)"
affects: [00-02-core-infrastructure, all future apps]

# Tech tracking
tech-stack:
  added: [next 16.2.12, react 19.2.4, tailwindcss 4, shadcn 4.16.1, radix-ui, lucide-react, @neondatabase/serverless 1.1.0, zod 4.4.3, jose 6.2.6, bcryptjs 3.0.3, next-themes 0.4.6, react-hook-form 7.84.0, @hookform/resolvers 5.6.0, server-only 0.0.1, sonner, vitest 4.1.10, tsx 4.23.1, jsdom 30, @testing-library/react 16.3.2, @testing-library/dom 10.4.1, @vitejs/plugin-react 6.0.5, vite-tsconfig-paths 6.1.1, tw-animate-css, clsx, tailwind-merge, class-variance-authority, shadcn]
  patterns:
    - "proxy.ts convention (Next 16) — plan 00-02 owns the file; scaffold is proxy-ready"
    - "shadcn v4 globals.css scaffold: tw-animate-css + shadcn/tailwind.css imports, @custom-variant dark, @theme inline"
    - "vitest server-only alias stub pattern (lib/session.ts, lib/db.ts import server-only)"
    - "UI-SPEC token authority: CLI defaults corrected post-init to canonical oklch values"

key-files:
  created: [package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, .gitignore, AGENTS.md, app/layout.tsx, app/page.tsx, app/globals.css, components.json, lib/utils.ts, components/ui/*.tsx (20), hooks/use-mobile.ts, vitest.config.mts, __tests__/setup-env.ts, __tests__/server-only-stub.ts, __tests__/smoke.test.tsx, .env.example]
  modified: []

key-decisions:
  - "scaffold moved via temp dir: create-next-app refuses non-empty dirs with .planning/ + PRD.md even with --yes; temp-dir fallback from the plan used"
  - "npm 11.18 EALLOWSCRIPTS blocks CLI-internal installs (CNA + shadcn init/add): deps installed manually, CLI re-run after deps present"
  - "shadcn 4.16.1 init flow has no base-color prompt: preset Nova (neutral) written; baseColor corrected to zinc in components.json; canonical zinc/indigo tokens enforced in globals.css per Token authority clause"
  - "components.json style is radix-nova (4.16.1 preset name); 'new-york' no longer exists in the CLI — documented deviation"
  - "AGENTS.md not generated by CNA in this version — created manually as template conventions doc"
  - ".gitignore .env* pattern would ignore .env.example — added !.env.example negation so the canonical contract is committable"

patterns-established:
  - "Pattern 1: shadcn v4 init in an existing Next app = deps install may fail under npm 11.18 (--allow-scripts rejection); install deps manually, re-run CLI"
  - "Pattern 2: vitest must alias 'server-only' to a stub (import-time crash otherwise)"
  - "Pattern 3: UI-SPEC token values applied AFTER shadcn init (Pitfall 5 avoidance); CLI additions accepted"

requirements-completed: [TMPL-01, TMPL-06, DEPL-04]

coverage:
  - id: D1
    description: "Next.js 16.2.12 App Router scaffold (TS, ESLint, Tailwind v4, Turbopack, @/* alias) that builds, lints, and type-checks cleanly"
    requirement: TMPL-01
    verification:
      - kind: other
        ref: "npm run build && npm run lint && npx tsc --noEmit (all exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "shadcn/ui 4.16.1 initialized (radix base, Nova preset, CSS variables, RSC) with 19 UI-SPEC components registered and theme-ready"
    requirement: TMPL-01
    verification:
      - kind: other
        ref: "ls components/ui | wc -l = 19; components.json baseColor zinc"
        status: pass
    human_judgment: false
  - id: D3
    description: "globals.css carries UI-SPEC canonical tokens (indigo primary/ring oklch(0.511 0.262 276.966) light / oklch(0.673 0.182 276.935) dark, radius 0.75rem, zinc neutrals, dark variant, Geist fonts)"
    requirement: TMPL-06
    verification:
      - kind: other
        ref: "grep oklch(0.511 0.262 276.966) app/globals.css; grep --radius: 0.75rem; grep @custom-variant dark"
        status: pass
    human_judgment: false
  - id: D4
    description: "vitest 4.1.10 harness with jsdom, RTL, @/ alias resolution, and server-only stub — green smoke test"
    verification:
      - kind: unit
        ref: "__tests__/smoke.test.tsx#renders a shadcn Button via jsdom + RTL + @/ alias"
        status: pass
    human_judgment: false
  - id: D5
    description: ".env.example with exactly 9 canonical vars (both DATABASE_URLs, SESSION_SECRET >= 32 chars, 6 MOCK_* switches) — placeholders only"
    requirement: DEPL-04
    verification:
      - kind: other
        ref: "grep -c DATABASE_URL .env.example >= 2; grep -c SESSION_SECRET >= 1; grep -c '^MOCK_' >= 6"
        status: pass
    human_judgment: false

# Metrics
duration: 40min
completed: 2026-08-01
status: complete
---

# Phase 0 Plan 1: Project Scaffolding Summary

**Next.js 16.2.12 scaffold with pinned deps, shadcn/ui 4.16.1 design system (19 components, UI-SPEC canonical indigo/zinc tokens), proven vitest harness with server-only stub, and the 9-variable .env.example contract**

## Performance

- **Duration:** 40 min
- **Started:** 2026-08-01T17:09:00+08:00
- **Completed:** 2026-08-01T17:20:13+08:00
- **Tasks:** 3
- **Files modified:** 40+

## Accomplishments

- Next.js 16.2.12 App Router scaffold (TypeScript, ESLint, Tailwind v4, Turbopack, `@/*` alias) — build, lint, and `tsc --noEmit` all green
- All 16 research-pinned packages installed with exact versions (neon 1.1.0, zod 4.4.3, jose 6.2.6, bcryptjs 3.0.3, next-themes 0.4.6, react-hook-form 7.84.0, resolvers 5.6.0, server-only 0.0.1, vitest 4.1.10, tsx 4.23.1 + 6 latest-resolved pinned); no `ws`
- shadcn/ui 4.16.1 initialized (radix base, Nova preset = lucide/Geist) with all 19 UI-SPEC components registered (+tooltip as sidebar dependency)
- globals.css carries the UI-SPEC canonical token set: indigo primary/ring (light + dark), 0.75rem radius, zinc neutrals, full sidebar token set, `@custom-variant dark`, Geist font mapping — shadcn v4 scaffold structure (tw-animate-css + shadcn/tailwind.css imports accepted)
- vitest harness proven: jsdom + RTL + vite-tsconfig-paths + `server-only` → stub alias; green smoke test; `npm run test` script
- `.env.example` committed with exactly the 9 canonical vars (placeholders only); `.gitignore` covers `.env*` with `!.env.example`
- AGENTS.md with the template's hard conventions for future apps

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold Next.js 16 app and install all pinned dependencies** - `e9accc8` (feat)
2. **Task 2: Initialize shadcn/ui (zinc) and register the 19 UI-SPEC components; apply canonical tokens** - `9308fdf` (feat)
3. **Task 3: Prove the vitest harness and commit the canonical .env.example** - `71adb85` (feat)

## Files Created/Modified

- `package.json` / `package-lock.json` - CNA scaffold + pinned deps + `test` script
- `app/layout.tsx` - Geist fonts, `suppressHydrationWarning`, `lang="en"`
- `app/globals.css` - UI-SPEC canonical token set (shadcn v4 scaffold shape)
- `components.json` - radix-nova style, zinc baseColor, RSC, CSS variables
- `lib/utils.ts` - `cn()` helper (clsx + tailwind-merge)
- `components/ui/*.tsx` (20 files) - all UI-SPEC components + tooltip
- `hooks/use-mobile.ts` - rewritten with `useSyncExternalStore` (lint fix)
- `vitest.config.mts` - jsdom + react + tsconfig paths + server-only alias
- `__tests__/setup-env.ts` - 9 canonical env vars for tests
- `__tests__/server-only-stub.ts` - `export {}` stub
- `__tests__/smoke.test.tsx` - RTL Button render test
- `.env.example` - 9 canonical vars with comments
- `.gitignore` - `!.env.example` negation added
- `AGENTS.md` - template conventions doc
- `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`, `public/*` - CNA defaults

## Decisions Made

- **Temp-dir scaffold fallback** — CNA refuses non-empty dirs (`.planning/`, `PRD.md` present) even with `--yes`; scaffolded into a temp dir and copied in per the plan's fallback step
- **Manual dep installs for CLI-internal failures** — npm 11.18's `EALLOWSCRIPTS` rejects the `--allow-scripts` flag CNA and shadcn pass internally; deps installed manually, then CLI re-runs succeeded
- **`radix-nova` style in components.json** — shadcn 4.16.1 replaced the `new-york` style with presets; Nova matches UI-SPEC's lucide/Geist choices
- **zinc baseColor + canonical tokens enforced** — 4.16.1 init has no base-color prompt (preset flow); per the UI-SPEC Token authority clause, `baseColor: "zinc"` set in components.json and the full zinc/indigo oklch token set written to globals.css
- **AGENTS.md created manually** — CNA's `--yes` flow did not generate it in this version; written as the template conventions doc (plan done-criteria requires it)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] CNA refused the non-empty repo root**
- **Found during:** Task 1
- **Issue:** create-next-app exits on non-empty dirs containing `.planning/` + `PRD.md` even with `--yes` (interactive prompt cannot be piped — non-TTY)
- **Fix:** Used the plan's documented fallback: scaffolded into `%TEMP%/opencode/cna`, copied generated files into the root (excluding `.git`/`node_modules`), removed copied `node_modules`, reinstalled
- **Files modified:** (process-level; no plan file changed)
- **Verification:** build/lint/tsc pass in the repo root
- **Committed in:** `e9accc8`

**2. [Rule 3 - Blocking] npm 11.18 `EALLOWSCRIPTS` rejects CLI-internal installs**
- **Found during:** Tasks 1-2 (CNA install, shadcn init deps, shadcn add deps)
- **Issue:** npm 11.18 rejects `--allow-scripts` in project-scoped installs; CNA and shadcn CLI both pass that flag internally → their install steps fail
- **Fix:** Ran the same dependency sets manually with plain `npm install` (runtime pinned deps, dev pinned deps, shadcn init deps incl. `shadcn` package, `sonner`), then re-ran the CLI steps which succeeded
- **Files modified:** package.json, package-lock.json
- **Verification:** `npm ls --depth=0` shows exact pins; shadcn add completed
- **Committed in:** `e9accc8`, `9308fdf`

**3. [Rule 2 - Missing Critical] shadcn init aborted before writing globals.css / lib/utils.ts**
- **Found during:** Task 2
- **Issue:** init died at its dep-install step, so globals.css stayed at the CNA default (no tokens, no dark variant) and `lib/utils.ts` was missing → build failed on `@/lib/utils`
- **Fix:** Wrote the full shadcn v4 globals.css scaffold (imports, `@custom-variant dark`, `@theme inline`, `:root`/`.dark`, `@layer base`) with UI-SPEC canonical tokens; created `lib/utils.ts` with `cn()`
- **Files modified:** app/globals.css, lib/utils.ts
- **Verification:** `npm run build` green; token greps pass
- **Committed in:** `9308fdf`

**4. [Rule 2 - Missing Critical] `.gitignore` would have excluded `.env.example`**
- **Found during:** Task 1 (gitignore audit)
- **Issue:** CNA's `.env*` pattern ignores `.env.example` too — the canonical contract could not be committed
- **Fix:** Added `!.env.example` negation
- **Files modified:** .gitignore
- **Verification:** `.env.example` committed in `71adb85`
- **Committed in:** `e9accc8`

**5. [Rule 1 - Bug] shadcn-generated `use-mobile.ts` failed `npm run lint`**
- **Found during:** Task 3 (lint check)
- **Issue:** `react-hooks/set-state-in-effect` error in the matchMedia effect pattern
- **Fix:** Rewrote with `useSyncExternalStore` (subscribe/getSnapshot/server snapshot)
- **Files modified:** hooks/use-mobile.ts
- **Verification:** `npm run lint` clean; build + tests still green
- **Committed in:** `71adb85`

**6. [Rule 2 - Missing Critical] AGENTS.md absent**
- **Found during:** Task 1 (done-criteria check)
- **Issue:** CNA 16.2.12's `--yes` flow did not generate AGENTS.md; plan done-criteria requires it (it ships with the template for future apps)
- **Fix:** Created AGENTS.md with the template's hard conventions (proxy.ts, force-dynamic, dual Neon URLs, server-only boundaries, bcrypt-in-handlers, raw SQL, migrations)
- **Files modified:** AGENTS.md
- **Verification:** file exists, committed
- **Committed in:** `e9accc8`

---

**Total deviations:** 6 auto-fixed (3 Rule 3 blocking, 2 Rule 2 missing-critical, 1 Rule 1 bug)
**Impact on plan:** All fixes were required to complete the planned work on npm 11.18 / shadcn 4.16.1; no scope creep. The `radix-nova` style string (vs `new-york`) is the only intentional config difference from the plan's manual check — it is the current CLI's equivalent preset and does not affect tokens or components.

## Issues Encountered

- CNA interactive prompts cannot be fed via stdin pipe (both the conflict prompt and prompt library) — temp-dir fallback was the reliable path
- shadcn 4.16.1's init flow differs from RESEARCH (preset selection replaces the base-color prompt; `-b radix` selects the component library) — handled via Token authority clause
- `vite-tsconfig-paths` emits a deprecation notice (Vite now supports tsconfig paths natively) — harmless; config kept as planned

## User Setup Required

None for this plan — `.env.local` with real Neon credentials is required by plan 00-02 Task 3 (checkpoint).

## Next Phase Readiness

- Pristine scaffold with all pinned deps, ready for `lib/validate.ts`, `lib/session.ts`, `lib/db.ts`, `proxy.ts` (plan 00-02)
- Vitest harness proven and inheritable by 00-02's TDD suites (validate, session, mock)
- `.env.example` is the exact contract `lib/validate.ts` will validate against
- **Blockers:** None for 00-02 Tasks 1-2; Task 3 onward needs the user's Neon project (checkpoint)

---
*Phase: 00-template-foundation*
*Completed: 2026-08-01*
