---
phase: 00-template-foundation
plan: 00-01
plan_name: "Project scaffolding — Next.js 16 + TypeScript + Tailwind v4 + shadcn/ui + test harness + .env.example"
status: draft
type: execute
wave: 1
depends_on: []
files_created:
  - package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, .gitignore
  - app/layout.tsx, app/page.tsx, app/globals.css
  - components.json, components/ui/*.tsx (19 shadcn components), lib/utils.ts
  - vitest.config.mts, __tests__/setup-env.ts, __tests__/server-only-stub.ts, __tests__/smoke.test.tsx
  - .env.example
files_modified: []
autonomous: true
requirements: [TMPL-01, TMPL-06, DEPL-04]
must_haves:
  truths:
    - A fresh Next.js 16 App Router app (TypeScript, Tailwind v4, Turbopack) builds, lints, and type-checks cleanly from the repo root
    - shadcn/ui 4.x is initialized with zinc base, CSS variables, RSC enabled; all 19 UI-SPEC components are registered and theme-ready
    - globals.css carries the UI-SPEC canonical tokens (indigo primary/ring, radius 0.75rem, zinc neutrals, dark variant, Geist fonts) — CLI additions accepted, token values authoritative
    - `npm run test` runs a passing vitest harness (config + env setup + smoke test) that later TDD tasks inherit
    - .env.example exists with the 9 canonical vars (both DATABASE_URLs, SESSION_SECRET, 6 MOCK_* switches) — placeholders only, never secrets
  artifacts:
    - package.json with pinned runtime deps (@neondatabase/serverless 1.1.0, zod 4.4.3, jose 6.2.6, bcryptjs 3.0.3, next-themes 0.4.6, react-hook-form, @hookform/resolvers, server-only) and dev deps (vitest stack, tsx)
    - components.json, components/ui/ (19 registered components)
    - app/globals.css with UI-SPEC tokens, app/layout.tsx with Geist fonts + suppressHydrationWarning
    - vitest.config.mts + __tests__/setup-env.ts + __tests__/smoke.test.tsx
    - .env.example
  key_links:
    - globals.css tokens must match UI-SPEC Color/Radius tables exactly (Token authority clause) — future apps inherit them
    - .env.example must match RESEARCH Topic 7 canonical contents (dual Neon URLs, SESSION_SECRET ≥32 chars, MOCK_* defaults) — lib/validate.ts in plan 00-02 validates against exactly these names
    - vitest config must resolve `@/` aliases (vite-tsconfig-paths) so 00-02 test files import lib/ modules
---

<objective>
Scaffold the `nextjs-starter` template foundation: Next.js 16.2.12 (App Router, TypeScript, Tailwind v4, Turbopack) via create-next-app, shadcn/ui 4.x initialized with the UI-SPEC's prescribed parameters (zinc base, CSS variables, RSC), all 19 UI-SPEC components registered, UI-SPEC canonical tokens applied to globals.css, the vitest harness proven with a green smoke test, and `.env.example` committed as the environment contract for plan 00-02.

Purpose: Every one of the ~30 future apps inherits this scaffold — the version pins, proxy.ts-ready layout, token set, and test harness are the ground every later phase stands on. Getting them right now (per RESEARCH Topic 1/6/7 and the UI-SPEC init parameters) prevents the 10 documented pitfalls at the source.

Output: A buildable, lint-clean Next.js app with the full shadcn design system, working test infra, and the canonical .env.example.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 0 goal, success criteria 1–5)
@.planning/phases/00-template-foundation/00-CONTEXT.md (locked decisions — raw SQL, shadcn, theme)
@.planning/phases/00-template-foundation/00-RESEARCH.md (Topics 1, 6, 7 — authoritative: verified commands, version pins, pitfalls 1/5/7)
@.planning/phases/00-template-foundation/00-UI-SPEC.md (init parameters, token authority, Component Inventory, Typography)
</context>

<tasks>

<task type="auto" id="00-01-01">
  <name>Task 1: Scaffold Next.js 16 app and install all pinned dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, .gitignore, app/layout.tsx, app/page.tsx, app/globals.css, public/*</files>
  <action>
    Run `npx create-next-app@latest . --yes` in the repo root C:\Users\Admin\Desktop\project\nextjs-starter (the directory contains .git/, .planning/, PRD.md — if the non-empty-dir prompt blocks, answer "yes" to continue; fallback: scaffold into a temp dir and move the generated files into the root, never touching .planning/ or PRD.md). Per RESEARCH Topic 1: --yes defaults = TypeScript, ESLint, Tailwind CSS, App Router, Turbopack, import alias @/*, AGENTS.md (keep AGENTS.md — it ships with the template per RESEARCH). If prompted interactively, answer React Compiler = No (template stability, per RESEARCH Topic 1 Gotchas).

    Then install runtime deps PINNED to the RESEARCH Standard Stack versions (floating ranges forbidden — T-00-03): npm install --save-exact @neondatabase/serverless@1.1.0 zod@4.4.3 jose@6.2.6 bcryptjs@3.0.3 next-themes@0.4.6 server-only@0.0.1 react-hook-form@7.84.0 @hookform/resolvers@5.6.0. Then dev deps (Topic 1 step 5 + Topic 6): npm install -D --save-exact vitest@4.1.10 tsx@4.23.1 @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths — for the five packages RESEARCH leaves at "(latest)" (@vitejs/plugin-react, jsdom, @testing-library/react, @testing-library/dom, vite-tsconfig-paths), pin each to its exact registry version resolved at install time (npm view <pkg> version) so package.json records exact versions — no ^ ranges anywhere (--save-exact is the safety net).

    Hard rules: do NOT install `ws` (Node 24 global WebSocket — RESEARCH Topic 2); do NOT manually bump typescript beyond the create-next-app pin (registry 7.0.2 is NOT the target — RESEARCH Topic 1 Gotchas); do NOT add tailwind.config.js (Tailwind v4 is CSS-first — RESEARCH Topic 1); leave next.config.ts default.

    Do NOT create proxy.ts or any lib/ files in this task — plan 00-02 owns them. Do NOT create .env.local (plan 00-02 checkpoint). This task only produces the pristine scaffold + installed deps.
  </action>
  <verify>
    <automated>cd C:\Users\Admin\Desktop\project\nextjs-starter && npm run build && npm run lint && npx tsc --noEmit</automated>
    <manual>npm ls @neondatabase/serverless zod jose bcryptjs next-themes react-hook-form @hookform/resolvers server-only vitest tsx --depth=0 shows the pinned versions (1.1.0 / 4.4.3 / 6.2.6 / 3.0.3 / 0.4.6 / 7.84.0 / 5.6.0 / 0.0.1 / 4.1.10 / 4.23.1); no `ws` in node_modules</manual>
  </verify>
  <done>App scaffolds cleanly in the repo root; `npm run build`, `npm run lint`, and `npx tsc --noEmit` all pass with zero errors; package.json carries the exact pinned deps above; AGENTS.md exists; .planning/ and PRD.md untouched.</done>
</task>

<task type="auto" id="00-01-02" depends_on="00-01-01">
  <name>Task 2: Initialize shadcn/ui (zinc) and register the 19 UI-SPEC components; apply canonical tokens</name>
  <files>components.json, lib/utils.ts, app/globals.css, components/ui/*.tsx, app/layout.tsx</files>
  <action>
    Run `npx shadcn@latest init` (existing-project path — do NOT use `-t next` which scaffolds a new project, per RESEARCH Topic 1 step 3). Answer the prompts per UI-SPEC init parameters (canonical, not negotiable): base color = zinc, css variables = yes, RSC = yes. Note: shadcn 4.x init is interactive — there is no --base-color flag; the executor must answer the prompts or pipe the answers.

    Then register the full UI-SPEC Component Inventory (19 components): `npx shadcn@latest add button input label card form textarea select table badge alert dialog alert-dialog dropdown-menu avatar skeleton separator sonner sheet sidebar`. The sidebar component pulls in its own deps automatically per UI-SPEC.

    After init+add, apply the UI-SPEC Token authority clause to app/globals.css:
    - ACCEPT the CLI-added imports (@import "tailwindcss", @import "tw-animate-css", @import "shadcn/tailwind.css") — RESEARCH Topic 1 Gotcha 5; do not remove them.
    - ENFORCE the UI-SPEC canonical values where CLI defaults differ: --primary and --ring = oklch(0.511 0.262 276.966) in :root and oklch(0.673 0.182 276.935) in .dark; --radius: 0.75rem; zinc neutrals for background/card/popover/secondary/muted/accent/border/input per the UI-SPEC Color table (light and dark); destructive per UI-SPEC; sidebar token set per UI-SPEC.
    - CONFIRM @custom-variant dark (&:is(.dark *)); exists (class-based dark mode) and @theme inline maps the token variables, plus --font-sans/--font-mono mapping.
    - Keep the @layer base rules from the shadcn v4 scaffold (* { @apply border-border outline-ring/50; } and body { @apply bg-background text-foreground; }).

    Fonts per UI-SPEC Typography: wire Geist Sans (UI) + Geist Mono (code) via next/font in app/layout.tsx, expose as --font-sans / --font-mono (the create-next-app template may already do this — if so, verify the variable names match what globals.css @theme inline references; adjust either side so the mapping resolves). Set <html lang="en" suppressHydrationWarning> (suppressHydrationWarning is REQUIRED for next-themes later — UI-SPEC Accessibility item 7; harmless now).

    Do NOT create theme-provider/theme-toggle or any page UI beyond the template landing — plan 00-02 Task 8 owns the design-system wiring. Do NOT create lib/validate.ts (00-02 Task 1).
  </action>
  <verify>
    <automated>cd C:\Users\Admin\Desktop\project\nextjs-starter && npm run build</automated>
    <manual>components.json exists at repo root with "style": "new-york" and "baseColor": "zinc"; ls components/ui | wc -l >= 19; grep primary token in app/globals.css returns oklch(0.511 0.262 276.966); grep -- "--radius" returns 0.75rem; grep "@custom-variant dark" returns the class variant line</manual>
  </verify>
  <done>All 19 shadcn components registered and compiling; globals.css carries the exact UI-SPEC token values (indigo primary/ring, 0.75rem radius, zinc neutrals, dark variant); Geist fonts resolve to --font-sans/--font-mono; `npm run build` passes.</done>
</task>

<task type="auto" id="00-01-03" depends_on="00-01-02">
  <name>Task 3: Prove the vitest harness and commit the canonical .env.example</name>
  <files>vitest.config.mts, __tests__/setup-env.ts, __tests__/server-only-stub.ts, __tests__/smoke.test.tsx, .env.example, package.json (test script)</files>
  <action>
    Create vitest.config.mts exactly per RESEARCH Topic 6 (Next.js official Vitest guide): defineConfig with plugins [tsconfigPaths(), react()] (import from 'vitest/config', '@vitejs/plugin-react', 'vite-tsconfig-paths'), test.environment = 'jsdom', and setupFiles: ['__tests__/setup-env.ts']. PLUS the checker-verified server-only stub (vitest 4.1.10 crashes at import time on the real "server-only" package): add resolve: { alias: { "server-only": fileURLToPath(new URL("./__tests__/server-only-stub.ts", import.meta.url)) } } inside the defineConfig (import fileURLToPath from "node:url" — this yields the absolute path to the stub), and create __tests__/server-only-stub.ts containing only `export {}`. This is REQUIRED so 00-02's TDD suites work: lib/session.ts and lib/db.ts start with `import "server-only"` and would crash vitest at import time otherwise. Do NOT use resolve.conditions: ["react-server"] for this — empirically it does NOT fix the crash. (Fallback if the alias ever misbehaves: vi.mock("server-only", () => ({})) in __tests__/setup-env.ts.)

    Create __tests__/setup-env.ts: sets the 9 canonical env vars on process.env BEFORE any test module imports them — DATABASE_URL and DATABASE_URL_DIRECT (valid https-style postgres URLs are fine as test placeholders), SESSION_SECRET (a 32+ char string), and MOCK_PAYMENT/EMAIL/SMS/OAUTH/MAPS/STORAGE = "mock". This is REQUIRED so that plan 00-02's lib/validate.ts (which fails fast at import) and lib/session.ts can be imported by tests. Also `afterEach` cleanup is NOT needed here (env vars are constants) — keep it minimal.

    Create __tests__/smoke.test.tsx: a minimal RTL smoke test (render a shadcn Button from components/ui/button and assert its text renders) that proves the jsdom + RTL + tsconfig-alias chain works. Add package.json script "test": "vitest run" (CI-friendly, no watch).

    Create .env.example with EXACTLY the canonical 9 vars and comments from RESEARCH Topic 7: DATABASE_URL (pooled postgresql://…-pooler…?sslmode=require), DATABASE_URL_DIRECT (direct, no -pooler), SESSION_SECRET=replace-with-32+byte-secret (comment: openssl rand -base64 32), and MOCK_PAYMENT/MOCK_EMAIL/MOCK_SMS/MOCK_OAUTH/MOCK_MAPS/MOCK_STORAGE=mock (comment: default mock; "real" reserved). Placeholder values ONLY — never real secrets (T-00-02). Verify .gitignore (from create-next-app) contains .env.local / .env*.local — never commit real env files.

    Do NOT create lib/validate.ts here — the env schema lives in plan 00-02 Task 1; .env.example is the documented contract it validates against.
  </action>
  <verify>
    <automated>cd C:\Users\Admin\Desktop\project\nextjs-starter && npm run test</automated>
    <manual>grep -c "DATABASE_URL" .env.example >= 2; grep -c "SESSION_SECRET" .env.example >= 1; grep -c "^MOCK_" .env.example >= 6; npm run build still passes</manual>
  </verify>
  <done>`npm run test` runs vitest and the smoke test passes; vitest.config.mts carries the "server-only" alias to __tests__/server-only-stub.ts (green harness that 00-02's TDD tasks inherit without import-time crashes — lib/session.ts, lib/db.ts, and lib/mock/* import "server-only"); .env.example contains exactly the 9 canonical variables with placeholders; `npm run build` and `npm run lint` still pass.</done>
</task>

</tasks>

## Dependencies

| Task | Depends On | Shared Files | Rationale |
|------|-----------|--------------|-----------|
| 00-01-01 | — | — | CNA scaffold + installs first; nothing else can run on an empty dir |
| 00-01-02 | 00-01-01 | app/globals.css, app/layout.tsx | shadcn init + add requires package.json/app present; init rewrites globals.css (Pitfall 5: init BEFORE manual theming) |
| 00-01-03 | 00-01-02 | package.json | test script + config after components exist (smoke test imports components/ui/button) |

All three tasks are sequential within plan 00-01. Plan 00-02 (wave 2) depends on this entire plan (shared files: package.json, app/layout.tsx, app/globals.css).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → npm registry | Untrusted third-party code enters the repo via install |
| Repo → git history | Secrets or placeholder-broken env files could be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01-SC | Tampering | npm/cargo/pip installs | high | mitigate | RESEARCH `## Package Legitimacy Audit` complete — all 16 packages Approved, zero [SUS]/[SLOP] actionable, `ws` intentionally dropped; no blocking human checkpoint required (no [ASSUMED] packages in the install set); executor verifies npm ls versions post-install |
| T-00-02 | Information Disclosure | .env.example, .gitignore | medium | mitigate | .env.example holds placeholders only ("replace-with-32+byte-secret"); real values never generated in this plan; .gitignore already covers .env*.local (verified in Task 3); secrets enter via .env.local only in plan 00-02 checkpoint |
| T-00-03 | Tampering | package.json versions | low | mitigate | Versions pinned per RESEARCH Standard Stack (verified against npm registry 2026-08-02); executor must NOT float versions or bump TypeScript beyond CNA pin (Pitfall: TS 7 not the target) |
| T-00-04 | Spoofing | globals.css tokens | low | mitigate | UI-SPEC Token authority clause enforced post-init (Pitfall 5): CLI-added imports accepted, canonical oklch values (indigo primary/ring, 0.75rem radius) applied after init, verified by grep in Task 2 |
</threat_model>

<verification>
Phase-level checks contributed by this plan:

```bash
cd C:\Users\Admin\Desktop\project\nextjs-starter
npm run build          # Next 16 production build (Turbopack) — must pass with zero errors
npm run lint           # eslint direct (Next 16 removed lint from build — RESEARCH Gotcha)
npx tsc --noEmit       # strict type check
npm run test           # vitest run — green smoke test
```

Manual greps (per task): components/ui count ≥ 19, primary token oklch(0.511 0.262 276.966), --radius 0.75rem, @custom-variant dark present, .env.example has 9 vars. `npm run dev` smoke: landing page renders, no console errors.
</verification>

<success_criteria>
## Success Criteria Mapping (plan 00-01 coverage)

| Phase SC | Covered Here | How |
|----------|--------------|-----|
| SC-4: env vars validated at startup | partial | .env.example establishes the exact 9 var names/contract (DEPL-04); actual Zod validation lands in 00-02 Task 1 (TMPL-10) |
| SC-5: .env.example, theme, states, CRUD | partial | .env.example + dark-variant CSS + next-themes dep + Geist fonts done here; toggle/pages/CRUD land in 00-02 Tasks 8–9 (TMPL-06/07/08) |
| SC-1 (auth), SC-2 (seed), SC-3 (mocks) | — | Fully in plan 00-02 |

## Requirements Coverage

- TMPL-01 (App Router + TS + Tailwind v4): Tasks 1–2
- TMPL-06 (dark/light foundation: CSS dark variant, next-themes installed, Geist): Tasks 2–3
- DEPL-04 (.env.example contract): Task 3
</success_criteria>

<output>
Create `.planning/phases/00-template-foundation/00-01-SUMMARY.md` when done (per execute-plan workflow).
</output>
